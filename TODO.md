# สิ่งที่เว็บขายของควรมี (เช็คลิสต์)

## ระบบสมาชิก / เข้าสู่ระบบ
- [x] สมัครสมาชิก/เข้าสู่ระบบ ด้วยอีเมล + รหัสผ่าน
- [x] เข้าสู่ระบบด้วย Google
- [ ] เข้าสู่ระบบด้วย Facebook (สร้างแอปแล้ว รอผ่าน Business Verification ของ Meta ก่อนเปิดให้ลูกค้าใช้
  — ปุ่มถอดออกจากหน้าเว็บชั่วคราว โค้ดยังอยู่ครบใน auth.js/login.html)
- [x] ลืมรหัสผ่าน (ส่งลิงก์รีเซ็ตทางอีเมล)
- [x] สลับภาษา ไทย / อังกฤษ ทุกหน้า
- [x] รองรับโหมดมืด (dark mode) — เปลี่ยนตามการตั้งค่าของเครื่องผู้ใช้เอง (แก้ให้ใช้ได้จริง 6 ก.ย. 2569)

## ระบบเครดิต / เติมเงิน
- [x] ลูกค้าเติมเงินเข้าเป็นเครดิต แล้วใช้เครดิตซื้อของ
- [x] แนบภาพสลิปทุกครั้งที่เติมเงิน (บังคับ)
- [x] เติมผ่านทรูมันนี่วอลเล็ต / ธนาคารกสิกรไทย / พร้อมเพย์ QR
- [x] เติมด้วยลิงก์ซองอั่งเปาทรูมันนี่ — **บอทกดรับให้อัตโนมัติ เครดิตเข้าทันที**
- [x] บอทกันใช้ซองซ้ำ + ยิงพร้อมกันหลายครั้ง
- [x] ทรูมันนี่ล่มชั่วคราว = ลูกค้ายิงซองเดิมใหม่ได้
- [x] กรณีเงินเข้าแล้วแต่บันทึกไม่ทัน = เด้งให้แอดมินตรวจสอบเอง
- [x] เครดิตเข้าเฉพาะเมื่อแอดมินอนุมัติ
- [x] แอดมินเพิ่มเครดิตให้สมาชิกคนไหนก็ได้
- [x] ลูกค้าดูประวัติเติมเงิน + ประวัติสั่งซื้อของตัวเอง
- [x] เครดิตอัปเดตทันทีที่แอดมินอนุมัติ (ไม่ต้องรีเฟรช)

## หลังบ้าน (แอดมิน)
- [x] แดชบอร์ด: ยอดขาย, ออเดอร์, สมาชิก, เครดิตคงเหลือรวม, รายการรออนุมัติ
- [x] กราฟยอดขายรายวัน / สมาชิกใหม่รายวัน / สินค้าขายดี
- [x] อนุมัติ–ไม่อนุมัติ ออเดอร์ (นับยอดขายเฉพาะที่อนุมัติแล้ว)
- [x] อนุมัติ–ไม่อนุมัติ การเติมเงิน + ดูภาพสลิป
- [x] เพิ่ม/แก้ไข/ลบสินค้า: ชื่อ, ราคา, รูปภาพ, สต๊อก, เปิด-ปิดขาย
- [x] คลังสินค้าดิจิทัลรายชิ้น (ใส่ไอดี/รหัสผ่านของแต่ละชิ้นเอง)
- [x] ปรับเครดิตขึ้น/ลง (ใส่ค่าติดลบเพื่อหักคืน)
- [x] รีเซ็ตยอดขายกลับเป็นศูนย์ (ยกเลิกได้ ไม่ลบประวัติลูกค้า)
- [x] จัดการสมาชิก: ดูเครดิต, เพิ่มเครดิต, ตั้ง/ถอดสิทธิ์แอดมิน
- [x] จำกัดสิทธิ์ทั้งหมดที่ Firestore Rules
- [x] ราคาสินค้าคิดที่ฝั่งเซิร์ฟเวอร์ ลูกค้าแก้ราคาก่อนสั่งไม่ได้
- [x] เตือนถ้ายอดออเดอร์ไม่ตรงราคาสินค้าปัจจุบัน
- [x] ปิดช่องยัดสคริปต์เข้าหน้าแอดมินผ่านสลิป/ลิงก์ซอง
- [x] จำกัดจำนวนครั้งที่ยิงบอท 8 ครั้ง/นาที/คน
- [x] แอดมินถอดสิทธิ์ตัวเองไม่ได้

## หน้าร้าน / สินค้า
- [x] แสดงรูปสินค้า, ราคา, สต๊อกคงเหลือ
- [x] แจ้ง "สินค้าหมด" และกดสั่งซื้อไม่ได้
- [x] ซื้อเกินสต๊อกไม่ได้
- [x] แบ่งหมวดหมู่สินค้า (ไอดีเกม / เติมเกม) — แท็บกรองเหนือรายการสินค้า
- [x] ของเติมเกม: ลูกค้ากรอกไอดีเกม/UID หรือ ชื่อผู้ใช้+รหัสผ่าน ตอนสั่งซื้อ (แอดมินติ๊กเลือกต่อสินค้า)
- [x] ค้นหาสินค้า + กรองตามราคา + เรียงลำดับ (ทำงานร่วมกับแท็บหมวดหมู่)
- [ ] หน้ารายละเอียดสินค้า (รูปหลายมุม, สินค้าที่เกี่ยวข้อง)
- [ ] หน้าแรกโชว์สินค้าเด่น/โปรโมชั่น

## ตะกร้า / สั่งซื้อ
- [x] แก้จำนวน/ลบสินค้าในตะกร้า
- [x] เตือนเมื่อเครดิตไม่พอ พร้อมลิงก์ไปเติมเงิน
- [x] เลขที่คำสั่งซื้อให้ลูกค้าอ้างอิง
- [x] หน้าประวัติการซื้อ — ซื้อแล้วหักเครดิต สินค้าเข้าประวัติทันที
- [x] สินค้าดิจิทัล: กดดูไอดี/รหัสผ่านที่ซื้อได้ในประวัติการซื้อ
- ~~กรอกที่อยู่/เบอร์สำหรับจัดส่ง~~ (ไม่ต้องใช้ ร้านขายของออนไลน์)
- ~~ค่าจัดส่ง~~ (ไม่ต้องใช้)

## ความน่าเชื่อถือ
- [x] ช่องทางติดต่อแอดมิน (ปุ่มลอยมุมจอทุกหน้า → เฟซบุ๊กแอดมิน)
- [x] เงื่อนไขการให้บริการ + นโยบายการคืนสินค้า (terms.html)
- [ ] รีวิว/ดาวจากลูกค้า

## ใช้งานง่าย
- [x] รองรับมือถือ (responsive) — ตรวจจอ 320-390px ทุกหน้าแล้ว ไม่มีจุดล้นขอบ
- [x] ย่อรูปอัตโนมัติก่อนอัปโหลด (โหลดเร็ว)
- [x] ปุ่ม "ติดต่อแอดมิน" ลอยตลอดเวลา (เพิ่มช่องทาง LINE ได้ที่ SHOP.contact.channels)

## การตลาด/SEO
- [x] ตั้งชื่อ/คำอธิบายเว็บให้ค้นหาเจอบน Google
- [x] Open Graph tags (แชร์ลิงก์แล้วโชว์รูปแบนเนอร์ + ชื่อร้าน)
- [ ] ป้ายโปรโมชั่น/ส่วนลด

---
## หมายเหตุการตั้งค่า
- ข้อมูลบัญชีรับเงินทั้งหมดแก้ได้ที่ไฟล์ `shop-config.js`
- บอทรับซองอั่งเปาอยู่ที่ Cloudflare Workers (โฟลเดอร์ `worker/`)
  - แก้โค้ดแล้ว deploy ใหม่: `cd worker && npx wrangler deploy`
  - เบอร์รับเงินเก็บเป็นความลับใน Cloudflare: `npx wrangler secret put RECEIVE_PHONE`
  - ปิดระบบอัตโนมัติ: ลบค่า `botUrl` ใน `shop-config.js` (กลับไปใช้แอดมินกดรับเอง)
- **สิทธิ์แอดมินไม่ได้ดูจากอีเมลแล้ว** (เลิกใช้ ADMIN_EMAILS / ownerEmails ตั้งแต่ 6 ก.ย. 2569)
  ตั้ง/ถอดสิทธิ์ที่หลังบ้าน แท็บสมาชิก · ถ้าไม่เหลือแอดมินเลย ใช้ปุ่ม "กู้คืนสิทธิ์แอดมินด้วยรหัสลับ"
  ในหน้า admin.html แล้วใส่รหัส ADMIN_BOOTSTRAP ที่เก็บไว้ใน Cloudflare

---
## รอบตรวจโค้ดใหญ่ (5 ก.ย. 2569) — แก้แล้วทั้งหมด
- [x] กด "ไม่อนุมัติ" ทับรายการที่อนุมัติไปแล้วได้ เครดิตหายไม่คืน
- [x] บอทรับซองสำเร็จแต่บันทึกไม่ทัน ลูกค้าเสียเงินฟรี
- [x] รายการยอด 0 กดอนุมัติแล้วเครดิตเข้า 0 บาทเงียบๆ
- [x] แอดมินอนุมัติตัดหน้าบอท เครดิตเข้าซ้ำสองเด้ง
- [x] เศษสตางค์เพี้ยน / หักเครดิตพร้อมกันแล้วติดลบ
- [x] ชื่อกับเบอร์ที่กรอกตอนสมัครหาย
- [x] สมาชิกที่ไม่มีวันที่สมัคร หายจากหลังบ้าน
- [x] ประวัติลูกค้าหยิบมามั่ว ไม่ใช่รายการล่าสุด
- [x] กด "เพิ่มชิ้น" แล้วสต๊อกขึ้นทันทีทั้งที่ยังไม่กรอกไอดี
- [x] ลบสินค้าแล้วไอดี/รหัสผ่านค้างในฐานข้อมูลตลอดไป
- [x] QR พร้อมเพย์สร้างเลขบัญชีผิดถ้าตั้งเบอร์แบบ 66.../+66...
- [x] ลูกค้าสร้างรายการเติมเงินเปล่าๆ ให้ดูเหมือนแอดมินเติมให้ได้
- [x] ลูกค้าแก้อีเมลตัวเองในฐานข้อมูลได้
- [x] หน้าแอดมินโหลด 57 MB / หน้าร้านโหลด 8.6 MB ก่อนแสดงอะไร
- [x] กดอนุมัติ 1 ครั้ง อ่านฐานข้อมูลพันกว่ารายการ (เหลือ 4)
- [x] เน็ตหลุดแล้วหน้าเว็บโชว์หน้าเปล่าเงียบๆ
- [x] ปลายทางค้าง แล้วหน้าจอค้างตามไม่มีกำหนด
- [x] ปิดกล่องลอยด้วย Esc / กดพื้นที่มืด + ล็อกหน้าหลังไม่ให้เลื่อน
- [x] ชุดทดสอบ 598 ข้อ (`cd tests && npm test`)

---
## ยังไม่ได้ทำ (คุยกันไว้)
- หน้ารายละเอียดสินค้า
- รีวิว/ดาวจากลูกค้า
- เข้าสู่ระบบด้วย Facebook (สร้างแอปแล้ว รอผ่าน Business Verification ของ Meta)
- **แผนรอบถัดไป 5 เรื่อง (ซื้อทันทีไม่ต้องรออนุมัติ) → อยู่ท้ายไฟล์นี้**

---
## รอบล่าสุด (5 ก.ย. 2569) — หมวดหมู่สินค้า + เก็บงานมือถือ
- [x] หมวดหมู่สินค้า: แท็บ ทั้งหมด / ไอดีเกม / เติมเกม เหนือรายการสินค้าหน้าร้าน
  - เพิ่ม/แก้หมวดได้ที่ `CATEGORIES` ใน shop-config.js ที่เดียว
  - หลังบ้านมีช่อง "หมวดหมู่" ในฟอร์มสินค้า + โชว์หมวดในรายการสินค้า
  - สินค้าเก่าที่ยังไม่ระบุหมวด ยังโผล่ในแท็บ "ทั้งหมด" ตามเดิม
- [x] ตารางหลังบ้านบนมือถือ แปลงเป็นการ์ดทีละรายการ (เดิมบีบจนอ่านไม่ออก ปุ่มอนุมัติหลุดนอกจอ)
- [x] การ์ดตัวเลขหลังบ้านเหลือ 2 ใบต่อแถวบนมือถือ (เดิมแถวละใบ ต้องเลื่อนยาว)
- [x] กราฟ 2 ใบเรียงลงมาทีละใบ (เดิมล้นขอบจอ 320px ทำให้ทั้งหน้าเลื่อนซ้ายขวาได้)
- [x] ชื่อสินค้าไทยยาวๆ ดันการ์ดกว้างเกินจอ (ใส่ min-width:0)
- [x] แถบแท็บที่เลื่อนได้ เฟดขอบขวาให้รู้ว่ายังมีต่อ (หน้าร้าน + หลังบ้าน)
- [x] admin.js เรียก closePanel แบบไม่ผ่าน window ทำให้รายการไม่รีเฟรชหลังบันทึก

---
## วิธีขายไอดีเกม (สินค้าดิจิทัล)
> เลือก **หมวดหมู่ = 🎮 ไอดีเกม** ในฟอร์มสินค้า สินค้าถึงจะไปโผล่ในแท็บไอดีเกมหน้าร้าน

1. หลังบ้าน → แท็บ **สินค้า** → เพิ่มสินค้า → ใส่ชื่อกับราคา → ติ๊ก **สินค้าดิจิทัล** → บันทึก
2. หน้าต่างจะเปิดต่อให้ กด **เพิ่มชิ้น** ตามจำนวนที่มี (เช่น 10 ชิ้น)
3. ใส่ชื่อผู้ใช้ / รหัสผ่าน / หมายเหตุ ของแต่ละชิ้นให้ต่างกัน → กด **บันทึก**
4. สต๊อกจะนับให้เองจากจำนวนชิ้นที่ยังไม่ถูกขาย
5. ลูกค้าซื้อ → คุณกด **อนุมัติ** ในแท็บออเดอร์ → ระบบจับของให้ลูกค้าอัตโนมัติ
   ลูกค้าเข้าประวัติการซื้อแล้วกดที่รายการ จะเห็นไอดีกับรหัสผ่านทันที
6. ชิ้นที่ขายไปแล้วจะขึ้นว่า "ขายแล้ว" แก้ไม่ได้และไม่ถูกขายซ้ำ

## วิธีขายของเติมเกม (เติมให้ลูกค้าเอง)
> เลือก **หมวดหมู่ = 💎 เติมเกม** และ **ห้ามติ๊ก "สินค้าดิจิทัล"**
> (สินค้าดิจิทัลใช้กับของที่มีไอดี/รหัสผ่านรายชิ้นเท่านั้น ของเติมเกมไม่มี)

1. หลังบ้าน → แท็บ **สินค้า** → เพิ่มสินค้า
2. ใส่ชื่อแพ็ก เช่น "เพชร 100 เม็ด" / ราคา / เลือกหมวดหมู่ **เติมเกม**
3. ช่อง **สต๊อก** — เว้นว่างไว้ = ขายได้ไม่จำกัด (เหมาะกับของเติมเกม)
   ถ้าอยากจำกัดจำนวนต่อวัน ค่อยใส่ตัวเลข
4. เลือกว่าจะ **ขออะไรจากลูกค้า** ตอนสั่งซื้อ (หัวข้อ "ข้อมูลที่ต้องขอจากลูกค้าตอนสั่งซื้อ")
   - ☑ **ไอดีเกม / UID** → เกมที่เติมด้วย UID อย่างเดียว (Free Fire, MLBB ฯลฯ)
   - ☑ **ชื่อผู้ใช้ + รหัสผ่านของลูกค้า** → เกมที่ต้องเข้าไอดีลูกค้าไปเติมให้
   - ติ๊กทั้งคู่ก็ได้ ถ้าเกมนั้นต้องใช้ทั้งสองอย่าง
5. ติ๊ก **เปิดขาย** → บันทึก
6. ลูกค้ากดสั่งซื้อ **ต้องกรอกช่องที่คุณติ๊กไว้ให้ครบก่อน** ไม่งั้นปุ่มสั่งซื้อกดไม่ได้
7. ข้อมูลจะโชว์ในออเดอร์หลังบ้าน (กล่องสีเหลือง) พร้อมปุ่มคัดลอก
8. คุณเติมเข้าเกมให้ → กด **อนุมัติ** ในแท็บออเดอร์ (เครดิตถึงจะถูกหัก)
9. เติมเสร็จแล้วกด **"ลบรหัสผ่านลูกค้า"** ในออเดอร์นั้น
   ระบบจะลบชื่อผู้ใช้/รหัสผ่านทิ้ง เหลือแค่ไอดีเกม/UID ไว้เป็นหลักฐาน
   **ทำทุกครั้งหลังเติมเสร็จ** จะได้ไม่เก็บรหัสผ่านลูกค้าไว้ในระบบ

---
## หน้าตาเว็บใหม่ (5 ก.ย. 2569)
- [x] เปลี่ยนธีมเป็นโทนส้ม-ทอง พื้นสว่าง การ์ดขาวมน เงานุ่ม (อ้างอิงแนว maat0.com)
- [x] ฟอนต์ Kanit (หัวข้อ/ตัวเลข) + Noto Sans Thai (เนื้อหา)
- [x] แบนเนอร์ต้อนรับหน้าร้าน พร้อมป้าย "ได้ของไว / ปลอดภัย / แอดมินตอบตลอด"
- [x] โลโก้แมว SVG (`logo.svg`) ที่หัวเว็บทุกหน้า
- [x] สติ๊กเกอร์การ์ตูน: แบนเนอร์ร้านเกม, น้องแมวหน้าเข้าสู่ระบบ, น้องโบกมือตอนไม่มีข้อมูล
  - ต้นฉบับจาก Gemini เป็น JPEG พื้นลายตาราง → ลบพื้นเป็นพื้นใส + ย่อขนาดแล้ว (รวม ~500 KB)
  - ไฟล์ต้นฉบับ `Gemini_Generated_Image_*` อยู่ใน .gitignore
- [x] โหมดมืดยังใช้ได้ (เปิดด้วย `<html data-theme="dark">`)

---
## รอบ SEO + แชร์ลิงก์สวย (5 ก.ย. 2569)
- [x] ทุกหน้ามีชื่อเรื่อง + คำอธิบายของตัวเอง (เดิมทุกหน้าชื่อ `QQSHOP` เหมือนกันหมด)
- [x] canonical ทุกหน้า — กัน Google นับหน้าเดียวกันซ้ำหลายที่อยู่
- [x] แชร์ลิงก์ในเฟซบุ๊ก/ไลน์/X แล้วขึ้นรูปแบนเนอร์ + ชื่อร้าน + คำโปรย (og: / twitter:)
  - รูปแชร์คือ `og-image.jpg` 1200x630 (แบนเนอร์ร้าน + แถบส้มชื่อ QQSHOP + น้องแมว)
- [x] ไอคอนแท็บเบราว์เซอร์ (favicon) — หัวน้องแมวบนพื้นส้มมุมมน
  - `favicon.svg` เวกเตอร์ (เบราว์เซอร์ใหม่) + `favicon-32/96.png` สำรอง
    + `favicon-180.png` ตอนบันทึกลงหน้าจอ iPhone
- [x] `sitemap.xml` บอก Google ว่ามีหน้าอะไรบ้าง (เฉพาะหน้าสาธารณะ)
- [x] `robots.txt` + `meta robots noindex` กันหน้ากระเป๋าเงิน/ประวัติ/หลังบ้าน ไม่ให้โผล่ในผลค้นหา
- [x] ข้อมูลร้านแบบ JSON-LD ในหน้าร้าน (ชื่อ/โลโก้/เวลาทำการ/ช่องทางจ่ายเงิน/เฟซบุ๊ก)
- [x] ชุดทดสอบใหม่ `tests/t21-seo.mjs` 168 ข้อ (รวมทั้งเว็บเป็น 766 ข้อ)

### ต้องทำทุกครั้งที่เพิ่มหน้าใหม่
1. ก๊อปบล็อก `<head>` จากหน้าที่มีอยู่ แล้วแก้ title / description / canonical / og:url
2. หน้าที่ต้องล็อกอินก่อนใช้ → ใส่ `<meta name="robots" content="noindex, nofollow">` ด้วย
3. หน้าสาธารณะ → เพิ่มใน `sitemap.xml`
4. เพิ่มชื่อไฟล์ใน `PAGES` ของ `tests/t21-seo.mjs` แล้วรัน `cd tests && npm test`

### เปลี่ยนรูปแชร์/ไอคอนใหม่
เปลี่ยน `hero-banner.jpg` หรือ `cat.png` แล้วสั่ง:
`cd tools && npm i --no-save jimp && node make-share-images.mjs`

### Google Search Console — ทำแล้ว (5 ก.ย. 2569)
- [x] ยืนยันความเป็นเจ้าของเว็บด้วยไฟล์ `google7927e7e3fa345a6c.html` (อยู่ในโฟลเดอร์หลัก **ห้ามลบ**)
- [x] ส่ง sitemap `https://908wayu-svg.github.io/qqshop/sitemap.xml`
- [x] ขอให้ Google เก็บหน้าร้านเข้าดัชนี (Request indexing)
- สถานะ sitemap ตอนส่งใหม่ๆ ขึ้นว่า "ดึงข้อมูลไม่ได้" เป็นเรื่องปกติ — ทดสอบด้วยเครื่องมือ
  ของ Google แล้วขึ้น "URL พร้อมใช้งานสำหรับ Google" สถานะจะเปลี่ยนเองภายใน 1-2 วัน
- เว็บใหม่ปกติใช้เวลาหลายวันถึงสัปดาห์กว่าจะค้นหาเจอบน Google
  เช็คได้โดยค้นคำว่า `site:908wayu-svg.github.io/qqshop` ใน Google
### ล้างแคชรูปแชร์ของเฟซบุ๊ก — ทำแล้ว (5 ก.ย. 2569)
- [x] สเครปใหม่ครบ 3 หน้าสาธารณะ (หน้าร้าน / login / privacy) ตัวอย่างลิงก์ขึ้นรูปแบนเนอร์ + ชื่อร้านถูกต้อง
  - privacy.html เคยถูกเฟซบุ๊กเก็บแคชไว้ตอนยังไม่มี og: (ตอนส่งให้ Meta ตรวจ) ล้างแล้ว
- ถ้าแก้รูปแชร์/ชื่อเรื่องอีกในอนาคต ต้องล้างแคชซ้ำ:
  https://developers.facebook.com/tools/debug/ → วางลิงก์ → กด "สเครปอีกครั้ง"
- ยังมีคำเตือน `fb:app_id` ค้างอยู่ (ไม่กระทบหน้าตาลิงก์ที่แชร์)
  ถ้าอยากให้หายต้องเอาเลขแอปจาก developers.facebook.com มาใส่เป็น meta ในทุกหน้า

---
## รอบค้นหาสินค้า (5 ก.ย. 2569)
- [x] ช่องค้นหาเหนือรายการสินค้า — พิมพ์ได้ทั้งชื่อไทย/อังกฤษ และคำในคำอธิบาย
  - พิมพ์หลายคำ = ต้องเจอครบทุกคำ ("เพชร 1000" ไม่เจอ "เพชร 100 เม็ด")
  - ปุ่มกากบาทในช่อง ล้างคำค้นได้ทันที
- [x] กรองช่วงราคา (ต่ำสุด/สูงสุด) — เว้นว่างช่องไหน = ไม่กรองด้านนั้น
  - ของราคา 0 บาทไม่หายตอนเว้นช่องว่าง (เคยเป็นบั๊กคลาสสิก)
  - ใส่ต่ำสุดมากกว่าสูงสุด ระบบบอกให้สลับตัวเลข ไม่ปล่อยให้งง
- [x] เรียงลำดับ 4 แบบ: ร้านจัดให้ / ถูก→แพง / แพง→ถูก / ชื่อ
- [x] ทำงานซ้อนกับแท็บหมวดหมู่ (ค้นในหมวดที่เลือกอยู่เท่านั้น)
- [x] บอกจำนวนที่พบ + แยกข้อความ "หาไม่เจอ" ออกจาก "ร้านยังไม่มีของ"
- [x] ปุ่มล้างตัวกรอง คืนค่าทุกช่องในทีเดียว
- [x] สลับภาษาแล้วคำค้น/ตัวกรองไม่หาย และข้อความในแถบแปลตามภาษา
- [x] จอ 320px ช่วงราคาลงมาเต็มบรรทัด ช่องยืดหดเอง ไม่ดันจอ
- [x] ชุดทดสอบใหม่ `tests/t22-search.mjs` 59 ข้อ + เพิ่มกฎมือถือใน t19 (รวมทั้งเว็บ 828 ข้อ)
- หมายเหตุ: ตัวกรองเก็บในหน่วยความจำอย่างเดียว เปิดหน้าใหม่ = เริ่มจากรายการเต็มเสมอ

---
## หน้าเงื่อนไข & การคืนสินค้า (5 ก.ย. 2569)
- [x] `terms.html` สองภาษา ไทย/อังกฤษ โครงเดียวกับหน้านโยบายความเป็นส่วนตัว
  - สั่งซื้อ/จ่ายด้วยเครดิต · ได้ของเมื่อไหร่ · เคลมได้กรณีไหน · ไม่รับประกันกรณีไหน
  - เครดิตในกระเป๋า · ข้อห้าม (สลิปปลอม/ซองไม่ใช่ของตัวเอง) · ข้อจำกัดความรับผิด
- [x] ลิงก์เข้าหน้านี้อยู่ในกล่อง "ติดต่อแอดมิน" (มีทุกหน้า) คู่กับนโยบายความเป็นส่วนตัว
- [x] ใส่ใน sitemap.xml + head SEO ครบ + ชุดทดสอบ `tests/t23-terms.mjs` 44 ข้อ (รวม 896 ข้อ)

### ตัวเลข/ข้อความที่ควรอ่านทวนแล้วบอกผมถ้าจะแก้
- **ระยะเวลาเคลม 24 ชั่วโมง** — แก้ได้ที่ `SHOP.policy.claimHours` ใน `shop-config.js` ที่เดียว
  (หน้าเว็บเปลี่ยนตามทุกจุดทั้งสองภาษา)
- **ขอคืนเครดิตที่ยังไม่ได้ใช้เป็นเงิน** — ตอนนี้เขียนว่า "ติดต่อแอดมิน พิจารณาเป็นรายกรณี"
  ถ้าอยากปิดตายว่าไม่คืนเลย หรือรับคืนเสมอ บอกได้
- **เครดิตไม่มีวันหมดอายุ** — ถ้าอยากตั้งวันหมดอายุ ต้องแก้ทั้งหน้านี้และระบบ

---
## ลำดับความสำคัญ — งานค้างทั้งหมด (จัดใหม่ 6 ก.ย. 2569)
> จำเป็นมาก → น้อย ตามความเสี่ยงเรื่องเงิน/ความปลอดภัย/ผลกระทบต่อลูกค้าจริง
> รายการที่ทำเสร็จแล้วย้ายไปอยู่ท้ายสุด เพื่อให้เห็นของที่ยังเหลืออยู่ชัดๆ

### ยังเหลืออยู่

**ต้องให้ผู้ใช้ตัดสินใจก่อน (ผมทำแทนไม่ได้)**
1. **OTP 2 ชั้นเข้าหลังบ้าน** — ติดตรงต้องสมัครผู้ให้บริการ SMS (ThaiBulkSMS ฯลฯ) ก่อน

**ทำได้เลย ไม่ต้องรออะไร**
2. **แบนสมาชิก** (`banned=true` แล้วซื้อ/เติมเงินไม่ได้) — เครื่องมือจำเป็นเมื่อลูกค้าเยอะขึ้น
   ต้องแก้ทั้งหน้าเว็บ · `firestore.rules` · เซิร์ฟเวอร์ (ทั้ง 3 ที่ ไม่งั้นกันไม่อยู่จริง)
3. **แก้ข้อความทุกหน้าได้เองจากหลังบ้าน** — งานใหญ่ที่สุดในลิสต์ ต้องรื้อ `i18n.js` ทั้งไฟล์
   ให้เป็น "ค่าเริ่มต้น + ทับด้วยค่าจากฐานข้อมูล" · เป็นแค่ความสะดวก ไม่กระทบเงิน/ความปลอดภัย
4. **ตัวกรองค้นหาสินค้าไม่จำค่าเวลาเปิดหน้าใหม่** — จุดอ่อนเล็กน้อยของหน้าร้าน
5. **หน้ารายละเอียดสินค้า** (รูปหลายมุม + สินค้าที่เกี่ยวข้อง)
6. **หน้าแรกโชว์สินค้าเด่น/โปรโมชั่น + ป้ายส่วนลด** — การตลาด ไม่เร่งด่วน
7. **รีวิว/ดาวจากลูกค้า** — การตลาด ไม่เร่งด่วน
8. **ปิดช่องลูกค้าสร้างเอกสารสลิปรัวๆ** — ต้องย้ายให้เซิร์ฟเวอร์รับสลิปแทน ซึ่งมีความเสี่ยงของตัวเอง
9. **`fb:app_id` ค้างเป็นคำเตือน** — ไม่กระทบหน้าตาจริง

**รอคนอื่น ไม่ใช่เรา**
10. **เข้าสู่ระบบด้วย Facebook** — รอ Meta อนุมัติ Business Verification

### ทำเสร็จแล้ว (6 ก.ย. 2569)
- **นโยบายคืนเครดิต** — ผู้ใช้ตัดสินใจแล้ว: เติมแล้วถอน/แลกคืนเป็นเงินไม่ได้ทุกกรณี ใช้ซื้อของในร้านได้อย่างเดียว
  แก้ข้อความใน `terms.html` ทั้งสองภาษาแล้ว (เดิมเขียนคลุมเครือว่า "พิจารณาเป็นรายกรณี")
- **สินค้าตัวอย่าง 1-4 บนเว็บจริง** — ผู้ใช้ยืนยันว่าเป็นของที่ตั้งใจใส่ไว้ก่อนตัดสินใจขายจริง
  และออเดอร์ที่มีอยู่เป็นเพื่อนผู้ใช้ทดลองซื้อเอง ไม่ต้องแก้อะไร
- ปิดทางเบราว์เซอร์แก้ `credit`/`role` โดยตรง · สิทธิ์แอดมินเป็น custom claim 2 ชั้น
- ลบรหัสผ่านลูกค้าอัตโนมัติตอนกด "ทำเสร็จแล้ว"
- สั่งซื้อทันที + หักเครดิตอัตโนมัติ (ไม่ต้องรออนุมัติ) · 3 สถานะออเดอร์ + ลูกค้าแก้ข้อมูลเองได้
- เงื่อนไขเคลม "แจ้งภายใน 10 นาที + มีวิดีโอ" ใน terms.html
- **ค้นออเดอร์ด้วยเลขที่คำสั่งซื้อ** (+ ปุ่มค้นทั้งฐานข้อมูลสำหรับออเดอร์เก่า)
- **ดูประวัติการซื้อของสมาชิกรายคน** (ดึงจากฐานข้อมูลตรงๆ)
- **แท็บบันทึกแอดมิน** (+ ช่องค้นในบันทึก)
- **ซ่อน/เลิกซ่อนรายการจากหน้าประวัติของลูกค้า** (`hiddenAt`)
---
# แผนรอบถัดไป — ซื้อทันที ไม่ต้องรออนุมัติ (บันทึก 5 ก.ย. 2569 · ยังไม่ได้ลงมือ)
> ผู้ใช้สั่งให้บันทึกไว้ก่อน แยกเป็น 5 เรื่อง ทำทีละเรื่องคนละรอบได้
> ลำดับที่ควรทำ: เรื่อง 1+2 ต้องทำคู่กัน → 3 → 4 → 5 (เรื่อง 5 ทำแยกก่อนก็ได้)
> กฎเหล็กข้อ 1, 2, 6, 10, 11 ยังใช้ทั้งหมด แค่ย้ายจุดหักเครดิตจาก "ตอนแอดมินอนุมัติ" มาเป็น "ตอนลูกค้ากดสั่งซื้อ"

## เรื่องที่ 1 — เลิกใช้ตะกร้า เปลี่ยนเป็นหน้าต่างสั่งซื้อทันที
- [x] กด "สั่งซื้อ" ที่การ์ดสินค้า → เปิดหน้าต่างสั่งซื้อของชิ้นนั้นเลย (ไม่มีตะกร้า ไม่มีหน้าตะกร้า)
- [x] ในหน้าต่างมี: ชื่อ/ราคา/จำนวน · ช่องกรอกไอดีเกม-UID (เฉพาะสินค้าเติมเกม) · ยอดรวม ·
      เครดิตคงเหลือ · **กล่องเงื่อนไขคืนสินค้า + ติ๊กยอมรับ** ถึงจะกดยืนยันได้
- [x] ข้อความเงื่อนไขในหน้าต่าง (ต้องมีทั้งไทย/อังกฤษใน i18n.js):
  - ต้องอัดวิดีโอตั้งแต่ **กดสั่งซื้อ จนเข้าเกมได้** (ไอดีเกม) / จนเพชรเข้าเกม (เติมเกม) ห้ามตัดต่อ ห้ามหยุดกลางคัน
  - เข้าเกมไม่ได้ / ไม่ได้เพชร ต้องแจ้งภายใน **10 นาที** เกินกว่านั้นร้านไม่รับประกัน
  - ไม่มีวิดีโอ = เคลมไม่ได้
- [x] เพิ่ม `SHOP.policy.claimMinutes = 10` + ข้อความเงื่อนไขวิดีโอ ใน `shop-config.js` ที่เดียว
      แล้วให้ทั้งหน้าต่างสั่งซื้อและ `terms.html` ดึงไปใช้ (ตอนนี้ terms ใช้ `claimHours` 24 ชม. — ต้องเขียนใหม่ให้ตรงกัน)
- [x] รื้อของเดิม: ตะกร้าใน localStorage · ปุ่ม/ตัวเลขตะกร้าบนหัวเว็บ · โค้ดตะกร้าใน app.js
- ไฟล์ที่แตะ: `index.html` `app.js` `style.css` `i18n.js` `shop-config.js` `terms.html` `tests/`
- **ตอบแล้ว (5 ก.ย. 2569):** จับเวลา 10 นาทีเริ่มนับตอน
  - ไอดีเกม: ทันทีที่ชื่อ+รหัสผ่านถูกส่งเข้าประวัติการซื้อของลูกค้า (คือตอนซื้อสำเร็จ เพราะจ่ายอัตโนมัติ)
  - เติมเกม: ทันทีที่สถานะออเดอร์เปลี่ยนเป็น **"สำเร็จ"** (ดูเรื่องที่ 3 — 3 สถานะ)
  → ต้องเก็บเวลาเริ่มนับ (`claimTimerStartedAt`) ไว้ในออเดอร์ แล้วให้ประวัติการซื้อ/หน้าออเดอร์คำนวณนับถอยหลังจากค่านี้
- **ตอบแล้ว:** เลือกจำนวนได้ (ไม่ใช่ทีละ 1 ชิ้นเท่านั้น) — หน้าต่างสั่งซื้อต้องมีช่องเลือกจำนวน
  ตรวจสต๊อกพอ/ไม่พอตามจำนวนที่เลือกด้วย

## เรื่องที่ 2 — หักเครดิต + จ่ายของอัตโนมัติ (ไม่ต้องรอแอดมินอนุมัติ)
- [x] **ไอดีเกม (digital):** Worker `/order` ทำใน transaction เดียว — เช็คเครดิตพอ → หักเครดิต →
      จับ `stockItems` ที่ available ตัวแรก → mark sold → คัดลอก login/password เข้าออเดอร์ →
      status = **completed** ทันที → ลูกค้ากดดูไอดีในประวัติได้เลย
- [x] **เติมเกม:** หักเครดิตทันทีเหมือนกัน แต่ status = **pending** รอแอดมินกดว่าเติมเสร็จ
- [x] ยังต้องผ่าน Worker เท่านั้น ห้ามให้เบราว์เซอร์หักเครดิต/สร้างออเดอร์เอง (กฎเหล็กข้อ 1-2)
- [x] แท็บออเดอร์หลังบ้านเปลี่ยนความหมาย: จาก "อนุมัติ/ไม่อนุมัติ" → "**ทำเสร็จแล้ว**" + "**ยกเลิก+คืนเครดิต**"
- [x] ปุ่มคืนเครดิตต้องเช็คสถานะใน transaction กันกดซ้ำแล้วคืนซ้ำ (กฎเหล็กข้อ 10) + ปัด 2 ตำแหน่ง (ข้อ 11)
- [x] สต๊อกหมดพอดีตอนกดซื้อ → transaction ต้อง fail ทั้งชุด ไม่หักเครดิต และเด้งข้อความบอกลูกค้า
- ไฟล์ที่แตะ: `worker/` `auth.js` `purchases.js` `admin.js` `firestore.rules` `tests/fake/store.mjs`

## เรื่องที่ 3 — สถานะออเดอร์ + ลูกค้าแก้ข้อมูลเองในประวัติ (เฉพาะเติมเกม)
- **ตอบแล้ว (5 ก.ย. 2569): ใช้ 3 สถานะ**
  1. **รอดำเนินการ** — ออเดอร์เข้าคิว รอแอดมินหยิบ
  2. **กำลังดำเนินการ** — แอดมินกดติ๊กว่าเริ่มเติมแล้ว (ฝั่งลูกค้าเห็นเปลี่ยนเป็นสถานะนี้)
  3. **สำเร็จ** — แอดมินกดว่าเติมเสร็จ → เริ่มนับเวลาเคลม 10 นาทีจากจุดนี้ (ดูเรื่องที่ 1)
- [x] ประวัติการซื้อขึ้นตามสถานะข้างบน (ไทย/อังกฤษใน i18n.js)
- [x] ลูกค้าแก้ไอดีเกม/UID/ชื่อผู้ใช้ ในประวัติได้ **เฉพาะตอนสถานะ "รอดำเนินการ" เท่านั้น**
      พอแอดมินกดเข้า "กำลังดำเนินการ" ช่องแก้ไขหายไปทันที (ล็อกตั้งแต่แอดมินเริ่มเติม ไม่ใช่รอจน "สำเร็จ")
- [x] ขึ้นข้อความเตือนว่า ถ้าแก้ช้าแล้วแอดมินเริ่มเติมเข้าไอดีเดิมไปแล้ว ร้านไม่รับผิดชอบ
- [x] เก็บประวัติการแก้ (เวลา + ค่าเดิม) ให้แอดมินเห็นในออเดอร์ กันเถียงกันทีหลัง
- [x] **เปลี่ยนวิธี: ทำผ่าน Worker `/order/edit-info` แทนการเปิดกฎ Firestore**
      *เหตุผลที่ไม่เปิดกฎ: ข้อมูลพวกนี้อยู่ใน `items[]` ซึ่งเป็นอาร์เรย์ กฎ Firestore ตรวจทีละสมาชิกไม่ได้
      ถ้าเปิดให้เขียนฟิลด์ items ลูกค้าก็แก้ qty/price ที่อยู่ในอาร์เรย์เดียวกันได้ด้วย = สั่ง 1 ชิ้นแล้วแก้เป็น 999 ชิ้น
      กฎ orders จึงยังปิดสนิทเหมือนเดิม (`allow create, update, delete: if false`) — `firestore.rules` และ
      `tests/fake/store.mjs` ไม่ต้องแก้เลย*
- [x] หลังบ้านแท็บออเดอร์ต้องมีปุ่มแยก 2 ปุ่ม: "เริ่มดำเนินการ" (รอ→กำลังทำ) และ "ทำเสร็จแล้ว" (กำลังทำ→สำเร็จ)
      ทั้งคู่เช็คสถานะปัจจุบันใน transaction ก่อนเปลี่ยน (กฎเหล็กข้อ 10 กันกดข้ามขั้น/กดซ้ำ)
- ไฟล์ที่แตะ: `purchases.html` `purchases.js` `firestore.rules` `tests/fake/store.mjs` `admin.js`

## เรื่องที่ 4 — หลังบ้านเพิ่มความสามารถ
- [x] ดูประวัติการซื้อของสมาชิกรายคน (กดจากแท็บสมาชิก) — ทำแล้ว 6 ก.ย. 2569
- [x] ค้นหาออเดอร์ด้วย **เลขที่คำสั่งซื้อ** ที่ลูกค้าแจ้งมา — ทำแล้ว 6 ก.ย. 2569
- [ ] **แบนสมาชิก** — `banned=true` แล้วซื้อ/เติมเงินไม่ได้ ทั้งฝั่งหน้าเว็บและ `firestore.rules`
- [x] ลบ/ซ่อนรายการในประวัติของสมาชิก (ลูกค้าเปิดดูแล้วไม่เห็นรายการนั้นอีก) — ทำแล้ว 6 ก.ย. 2569
      → ควรทำเป็น **ซ่อน (hiddenAt)** ไม่ใช่ลบทิ้งจริง จะได้ยังตรวจย้อนหลัง/กันเถียงได้
- [ ] **แก้ข้อความบนเว็บได้เองจากหลังบ้าน — ทุกหน้า ทุกจุดที่เป็นตัวหนังสือ** (ตอบแล้ว 5 ก.ย. 2569 ไม่ใช่แค่หน้าแรก)
      เช่นคำโปรยหน้าแรก "ไอดีเกมแท้ราคาถูก จ่ายด้วยเครดิต ได้ไอดีทันทีหลังแอดมินอนุมัติ" (ต้องแก้อยู่แล้ว
      เพราะเรื่องที่ 2 ทำให้ไม่มี "รอแอดมินอนุมัติ" อีกต่อไป) ไปจนถึงข้อความในหน้าอื่นๆ ทั้งหมด
      → เก็บใน `settings/texts` (key ต่อจุดข้อความ) แล้วทุกหน้าอ่านมาทับค่าเริ่มต้นใน i18n (ต้องมี 2 ภาษาทุก key)
      → หลังบ้านต้องมีหน้าจัดการข้อความแยก (แท็บใหม่ หรือหน้าใหม่) ลิสต์ทุก key พร้อมช่องแก้ไทย/อังกฤษ
      → งานนี้ใหญ่ เพราะ i18n.js ตอนนี้ค่าเริ่มต้น hardcode ในโค้ด ต้องรื้อให้ทุกจุดที่เคยเป็นข้อความคงที่
        กลายเป็น "ค่าเริ่มต้น + override จาก settings/texts ถ้ามี" — ควรทำเป็นงานแยกท้ายสุดของรอบนี้
- ไฟล์ที่แตะ: `admin.html` `admin.js` `auth.js` `index.html` `wallet.html` `purchases.html` `login.html`
  `privacy.html` `terms.html` `i18n.js` `firestore.rules` (ทุกหน้าที่มีข้อความ)

## เรื่องที่ 5 — ความปลอดภัย (ทำแยกก่อนได้)
> เหลือแค่ **OTP 2 ชั้นเข้าหลังบ้าน** อย่างเดียว — ติดตรงต้องสมัครผู้ให้บริการ SMS ก่อน (ทำแทนไม่ได้)
- [x] **กันคนแอบเป็นแอดมิน / แอบเติมเครดิต** — ทำแล้ว 6 ก.ย. 2569
  - `credit` กับ `role` เปลี่ยนได้จากฝั่งเซิร์ฟเวอร์เท่านั้น ปิดทางเบราว์เซอร์ทั้งหมด **แม้แต่แอดมินเอง**
  - ปุ่มปรับเครดิต/อนุมัติเติมเงินในหลังบ้าน → ยิงผ่าน Worker แล้วให้ Worker ตรวจ token ว่าเป็นแอดมินจริง
  - เปลี่ยนจากเช็ครายชื่ออีเมลใน rules มาใช้ **custom claim (`admin:true`)**
    *เหตุผล: ตอนนี้รายชื่ออีเมลอยู่ 2 ที่ (auth.js + firestore.rules) ใครยึดอีเมลนั้นได้ = เป็นแอดมินทันที*
  - บันทึก log ทุกครั้งที่เครดิตถูกปรับ (ใคร/เมื่อไหร่/เท่าไหร่/เหตุผล) ที่ `creditLogs` อ่านได้เฉพาะแอดมิน
- [x] **กันรหัสผ่านหลุด** — ทำครบแล้ว 6 ก.ย. 2569
  - `stockItems` ที่ยังไม่ถูกขาย ลูกค้าต้องอ่านไม่ได้เด็ดขาด (กฎเหล็กข้อ 6)
  - เห็นได้เฉพาะสำเนาในออเดอร์ของตัวเองหลังจ่ายของแล้วเท่านั้น
  - รหัสผ่านลูกค้า (เติมเกม) → **ลบอัตโนมัติ** เมื่อแอดมินกด "ทำเสร็จแล้ว" ✅ (ทำแล้ว 6 ก.ย. 2569)
  - ทดสอบยิงจริง **ด้วยบัญชีสมาชิกธรรมดา ไม่ใช่บัญชีแอดมิน** (rules simulator ไม่จับเคส list query)
- ไฟล์ที่แตะ: `firestore.rules` `worker/` `auth.js` `admin.js` `tests/`
- **ตอบแล้ว (5 ก.ย. 2569): เอา 2FA แบบ OTP ทาง SMS**
  - ทุกครั้งที่ล็อกอินเข้าหลังบ้านสำเร็จ (ผ่าน email/password หรือ Google แล้ว) ต้องกรอก OTP อีกชั้นก่อนเข้าได้จริง
  - OTP ส่งไปที่เบอร์คงที่ **091-820-0409** เสมอ ไม่ใช่เบอร์ของแต่ละแอดมิน (เบอร์เดียวคุมทุกบัญชีแอดมิน)
  - ออกรหัส 6 หลัก อายุสั้น (เช่น 5 นาที) + จำกัดจำนวนครั้งที่กรอกผิด กันบรute-force
  - ต้องทำฝั่ง Worker เท่านั้น (ขอ+ตรวจ OTP ไม่ทำในเบราว์เซอร์) เก็บ hash ของรหัสไว้ชั่วคราวใน Firestore/KV
    พร้อมเวลาหมดอายุ ไม่เก็บรหัสแบบข้อความล้วน
  - **ต้องเลือกผู้ให้บริการส่ง SMS ก่อนเริ่มทำจริง** (ต้องสมัครบัญชี+ได้ API key มาให้ ทำแทนไม่ได้)
    ตัวเลือกที่ใช้กับเบอร์ไทยได้: ThaiBulkSMS, Twilio (เบอร์ไทยได้แต่แพงกว่า), Firebase Phone Auth
    (ข้อจำกัด: Firebase Phone Auth ออกแบบมาให้ยืนยันเบอร์ผู้ใช้เอง ไม่เหมาะกับ "ส่งเข้าเบอร์เดียวตายตัว")
    → แนะนำ ThaiBulkSMS หรือผู้ให้บริการ SMS ไทยเจ้าอื่นที่มี API ส่ง OTP โดยตรง
- ไฟล์ที่แตะ: `worker/` `login.html` `admin.html` `admin.js` `auth.js` `firestore.rules` `tests/`

## ผลกระทบที่ต้องเก็บให้ครบทุกเรื่อง
- ชุดทดสอบ `tests/` ต้องแก้ตาม (ตอนนี้ 896 ข้อ) — โดยเฉพาะไฟล์ตะกร้า/ออเดอร์/rules
- ข้อความสองภาษาใน `i18n.js` ต้องครบทั้งไทย/อังกฤษทุกจุดที่เพิ่ม
- ตารางหลังบ้านที่เพิ่มคอลัมน์ ต้องใส่ `data-label` (กฎเหล็กข้อ 13)
- ตรวจจอ 320px ไม่ให้เลื่อนซ้ายขวา (กฎเหล็กข้อ 15)

---
## รอบความปลอดภัยเครดิต/สิทธิ์ (6 ก.ย. 2569) — ทำแล้ว
- [x] **เบราว์เซอร์เขียน `credit` / `role` ไม่ได้อีกแล้ว ทุกคน รวมถึงแอดมิน**
  กฎ users เหลือให้แก้ได้แค่ `name` กับ `phone` (มีจำกัดความยาวด้วย)
- [x] **งานแอดมินที่แตะเงินย้ายไปทำที่ Worker ทั้งหมด** (`/admin/*`)
  อนุมัติ/ไม่อนุมัติออเดอร์ · อนุมัติ/ไม่อนุมัติเติมเงิน · ปรับเครดิต · ลบข้อมูลลูกค้าในออเดอร์ · ตั้ง/ถอดสิทธิ์
  ใช้ transaction จริงของ Firestore ทุกคำสั่ง (กดพร้อมกันสองหน้าจอ เข้าได้ครั้งเดียว)
- [x] **สิทธิ์แอดมิน = custom claim + เอกสาร role ต้องครบทั้งคู่** (เลิกใช้รายชื่ออีเมลในโค้ด)
  ถอนสิทธิ์แล้วมีผลทันที ไม่ต้องรอโทเคนใบเก่าหมดอายุ
- [x] **บันทึกทุกการกระทำของแอดมินที่ `adminLogs`** (ใคร/เมื่อไหร่/เท่าไหร่/ยอดก่อน-หลัง)
  เบราว์เซอร์เขียนหรือลบไม่ได้เลย แอดมินอ่านได้อย่างเดียว
- [x] ปิดช่องเก่าเพิ่มเติมที่เจอระหว่างทาง:
  - ตอนสมัคร ใส่อีเมลเป็นของคนอื่นไม่ได้แล้ว + แนบฟิลด์แปลกปลอมไม่ได้
  - แอดมินสร้างรายการเติมเงิน "อนุมัติแล้ว" ปลอมๆ จากเบราว์เซอร์ไม่ได้แล้ว
  - แก้/ลบออเดอร์กับรายการเติมเงินจากเบราว์เซอร์ไม่ได้เลย (เดิมแอดมินทำได้)
  - ชื่อ/เบอร์ที่ลูกค้าแก้เอง จำกัดความยาว (กันยัดข้อความยาวผิดปกติเข้าหน้าแอดมิน)
- [x] ชุดทดสอบเจาะช่องโหว่ `tests/t24-admin-security.mjs` (78 ข้อ) + เทส Worker เพิ่ม (รวม 1052 ข้อ)
- [x] ทดสอบยิงจริงกับ Firebase ด้วยบัญชีทดสอบ แล้วลบทิ้งเรียบร้อย

### ของที่ต้องรู้ไว้
- **รหัสลับ ADMIN_BOOTSTRAP** เก็บอยู่ใน Cloudflare อย่างเดียว ใช้ตอน "ไม่เหลือแอดมินสักคน"
  เข้าหน้า admin.html แล้วกดปุ่ม "กู้คืนสิทธิ์แอดมินด้วยรหัสลับ"
- service account ต้องมีสิทธิ์ **Firebase Authentication Admin** ไม่งั้นตั้ง claim ไม่ได้
  (ให้สิทธิ์ไปแล้วเมื่อ 6 ก.ย. 2569)
- ถ้าเพิ่มคำสั่งแอดมินใหม่ ต้องทำที่ Worker เท่านั้น ห้ามกลับไปเขียนจากเบราว์เซอร์

---
## อ่านข้อมูลสลิปอัตโนมัติ (6 ก.ย. 2569) — ทำแล้ว
- [x] ตอนแอดมินกดดูสลิป (ทั้งรายการเก่าที่ฝังรูป และรายการใหม่ที่แยกเก็บ) ระบบอ่านตัวเลขในรูปให้อัตโนมัติ
  ด้วย Tesseract.js (ทำงานในเบราว์เซอร์ล้วนๆ ไม่ส่งรูปออกไปที่ไหน) โชว์ใต้รูปสลิป:
  - ยอดเงิน · วันที่ · เวลา · ชื่อผู้โอน (ถ้าอ่านเจอ)
- [x] มีคำเตือนกำกับตลอดว่า **อ่านอัตโนมัติอาจผิดได้ ต้องเทียบกับรายการโอนจริงในแอปธนาคารก่อนอนุมัติเสมอ**
  — เป็นแค่ตัวช่วยให้เทียบเร็วขึ้น **ไม่ใช่การยืนยันว่าสลิปเป็นของจริง**
- [x] อ่านไม่ออก/โหลดไลบรารีไม่ได้ (เน็ตมีปัญหา) ไม่กระทบการดูรูปสลิปตามปกติ ขึ้นข้อความแจ้งแทน
- [x] ชุดทดสอบ `tests/t25-slip-ocr.mjs` (15 ข้อ) ทดสอบตัวแยกข้อมูลด้วยข้อความตัวอย่าง (รวมทั้งเว็บ 1067 ข้อ)

### จุดที่ยังเป็นข้อจำกัด (บอกไว้ให้รู้)
- **อ่านตัวหนังสือได้ ไม่ได้แปลว่าสลิปเป็นของจริง** — คนตัดต่อรูปสลิปปลอมให้ตัวเลขสวยก็อ่านออกเหมือนกัน
  ถ้าอยากเช็คว่าสลิปเป็นของจริงจริงๆ (เทียบกับธนาคารตรง) ต้องใช้บริการ Slip Verification API แยกต่างหาก
  (เช่น SlipOK / EasySlip — มีค่าใช้จ่ายต่อครั้ง ต้องสมัครเอง) ยังไม่ได้ทำส่วนนี้
- รูปแบบสลิปแต่ละธนาคาร/แอปไม่เหมือนกัน อ่านชื่อผู้โอนพลาดได้ง่ายกว่ายอดเงิน/วันที่/เวลา
- ต้องโหลดไลบรารี OCR จากอินเทอร์เน็ตตอนกดดูสลิปครั้งแรก (ไม่ใช่ครั้งแรกที่เปิดหน้าเว็บ) อาจช้าสักครู่

---
## รอบรีวิวโค้ดตัวเอง #1 (6 ก.ย. 2569) — แก้แล้ว
> รีวิวโค้ดที่เขียนในรอบความปลอดภัย + รอบอ่านสลิป ทีละฟังก์ชัน แล้วแก้ทุกจุดที่เจอ

### ฝั่งเซิร์ฟเวอร์ (Worker)
- [x] `/admin/diagnose` เคยตรวจสิทธิ์ด้วยการ **เขียน claim เดิมทับตัวเอง** — ถ้าอ่านค่าเดิมมาไม่ครบ
  จะกลายเป็นล้างสิทธิ์ของคนที่กดตรวจทิ้ง → เปลี่ยนเป็น "อ่าน" อย่างเดียว ไม่เขียนอะไรเลย
- [x] ตั้งสิทธิ์แอดมินแล้วเขียนเอกสารไม่สำเร็จ → เคยทิ้ง claim ค้างไว้เป็นขยะ
  ตอนนี้ถอน claim กลับให้อัตโนมัติ (ทั้ง `/admin/role` และ `/admin/bootstrap`)
- [x] `NO_PROFILE` ไม่ได้อยู่ในรายการรหัสที่ส่งกลับได้ → กลายเป็น "ทำรายการไม่สำเร็จ (500)"
  ทั้งที่ควรบอกตรงๆ ว่ายังไม่มีเอกสารสมาชิก

### ฝั่งหน้าเว็บ
- [x] ตัวอ่านสลิปไม่มีเพดานเวลา — เน็ตช้า/หลุด = ค้างที่ "กำลังอ่าน..." ตลอดกาล (ใส่เพดาน 90 วินาที)
- [x] รหัสข้อผิดพลาดใหม่ที่ยังไม่มีคำแปล เคยโผล่เป็น `a_XXX` ให้ผู้ใช้เห็น → ถอยไปใช้ข้อความกลางแทน
- [x] `view_slip` ถูกประกาศซ้ำสองครั้งในไฟล์คำแปล (ตัวแรกตายเงียบๆ) → ลบตัวซ้ำ

### ให้เปิดได้ทุกเครื่อง/ทุกเบราว์เซอร์
- [x] `:has()` เขียนรวมอยู่ในลิสต์เดียวกับกฎธรรมดา — เบราว์เซอร์ที่ไม่รองรับจะทิ้ง **ทั้งกฎ**
  แยกออกจากกันแล้ว + เลิกใช้ `:has()` กับแถว "ยังไม่มีข้อมูล" (ใส่คลาสที่แถวแทน)
- [x] `backdrop-filter` ขาดคำนำหน้า `-webkit-` ที่ป้ายหน้าแรก (iOS ไม่เบลอให้)
- [x] ตรวจแล้วไม่มี: API ใหม่ที่เบราว์เซอร์เก่าไม่รองรับ · เส้นทางไฟล์แบบตายตัว · สคริปต์ที่ผูกกับ OS เดียว
  · เทสที่พังเพราะขึ้นบรรทัดใหม่แบบ Windows (CRLF)

### เพิ่มเครื่องมือกันพลาดซ้ำ
- [x] `tests/t26-i18n.mjs` — คีย์ไทย/อังกฤษต้องครบเท่ากัน · ห้ามมีคีย์ซ้ำ · ทุก `data-i18n` ในหน้าเว็บต้องมีคำแปลจริง
- [x] เพิ่มเทสเจาะกรณีที่เพิ่งแก้: diagnose ต้องไม่แตะ claim · ตั้งสิทธิ์ล้มเหลวต้องไม่ทิ้ง claim ค้าง
  · bootstrap ที่ไม่มีเอกสารสมาชิกต้องตอบ NO_PROFILE

### ยังไม่ได้แก้ (ตั้งใจ — ต้องตัดสินใจก่อน)
- **ลูกค้าสร้างเอกสารสลิป (`topupSlips`) รัวๆ ได้ไม่จำกัด** ใบละไม่เกิน 1MB
  กฎอนุญาตให้สร้างของตัวเองได้เสมอ (จำเป็น เพราะต้องแนบสลิปก่อนยื่นคำขอ)
  ถ้าจะปิดช่องนี้ต้องให้ Worker เป็นคนรับสลิปแทน = เปลี่ยนลำดับการเขียนซึ่งมีความเสี่ยงของตัวเอง
- ~~**บันทึก `adminLogs` ยังไม่มีหน้าให้ดูในเว็บ**~~ **ทำแล้ว 6 ก.ย. 2569** — แท็บ "บันทึกแอดมิน" ในหลังบ้าน

---
## รอบรีวิวโค้ดตัวเอง #2 (6 ก.ย. 2569) — แก้แล้ว
> รอบนี้ไล่ไฟล์ฝั่งลูกค้าที่ยังไม่ได้ตรวจลึก (app.js · wallet.js · purchases.js · contact.js · ui.js)

- [x] **ปุ่มคัดลอกในหลังบ้านกดแล้วไม่เกิดอะไรขึ้น** — หน้าหลังบ้านวาดปุ่มคัดลอกข้อมูลลูกค้า
  (ไอดีเกม/ชื่อผู้ใช้/รหัสผ่าน สำหรับเติมเกม) ไว้ แต่ไม่มีตัวรับคลิกเลยตั้งแต่แรก
- [x] ปุ่มคัดลอกใช้ `navigator.clipboard` อย่างเดียว — เบราว์เซอร์เก่า/เว็บวิวบางตัวใช้ไม่ได้ แล้วเงียบไปเฉยๆ
  ย้ายมาไว้ที่ `ui.js` ที่เดียว มีทางสำรอง และบอกผลเสมอ (✓ = คัดลอกแล้ว · ✕ = เบราว์เซอร์นี้ทำให้ไม่ได้)
- [x] **แถว "ยังไม่มีข้อมูล" ในหน้ากระเป๋าเงินเสียการจัดรูปแบบบนมือถือ** — รอบที่แล้วแก้ที่ admin.js
  แต่ลืม wallet.js ที่ใช้รูปแบบเดียวกัน (regression ที่เกิดจากการแก้รอบก่อน)
- [x] **ตัวจับเวลาเพดาน 90 วินาทีของตัวอ่านสลิปไม่ถูกเคลียร์** เมื่ออ่านเสร็จแล้ว
  (ค้างอยู่ทุกครั้งที่เปิดดูสลิป · ในชุดทดสอบทำให้ค้างรอ 90 วินาทีต่อครั้ง)
- [x] **สคริปต์จากเว็บนอกไม่มีการตรวจลายเซ็นไฟล์** — หน้าเติมเงินโหลดไลบรารีสร้าง QR จาก CDN
  ถ้า CDN ถูกแฮกแล้วสลับไฟล์ = เปลี่ยน QR/เลขบัญชีที่ลูกค้าเห็นได้
  ใส่ `integrity` + `crossorigin` ให้ทั้งตัวสร้าง QR และตัวอ่านสลิปแล้ว (โหลดไม่ได้ = ถอยไปใช้วิธีเดิม ไม่พังทั้งหน้า)

### เทสที่เพิ่ม
- [x] ปุ่มคัดลอกในหลังบ้านต้องคัดลอกค่าที่ถูกต้องจริง + บอกผลบนปุ่ม
- [x] เปิดดูสลิปแล้วต้องมีตัวเลขที่อ่านได้ + คำเตือนให้เทียบกับแอปธนาคาร
- [x] ทุกแถวว่างที่สร้างจาก JS ต้องมีคลาสที่ CSS มือถือใช้จริง (ทั้ง admin.js และ wallet.js)
- [x] สคริปต์ภายนอกทุกตัวต้องตรึงเวอร์ชัน + มี integrity
- [x] เทสไม่ยิงเน็ตจริงไปโหลดไลบรารี OCR อีกแล้ว (ชุดทดสอบเร็วขึ้นมาก)

รวมทั้งเว็บ 1104 ข้อ ผ่านหมด

---
## รอบรีวิวโค้ดตัวเอง #3 (6 ก.ย. 2569) — แก้แล้ว
> รอบนี้เน้นตรวจแบบกลไก (ให้เครื่องหาแทนตา) + ทดสอบบนเว็บจริงทุกหน้า

- [x] **รหัสซองอั่งเปาที่ยาวผิดปกติ ตอบสาเหตุผิด** — ลิงก์มั่วที่มีรหัสยาวเป็นร้อยตัว
  จะกลายเป็นชื่อเอกสารที่ Firestore ไม่รับ แล้วระบบตอบว่า "ซองนี้ถูกใช้ไปแล้ว"
  ทั้งที่จริงคือลิงก์ไม่ถูกต้อง → จำกัดความยาวรหัสไว้ที่ 64 ตัว

### เพิ่มเครื่องมือกันพลาด
- [x] `tests/t27-dom-wiring.mjs` — ตรวจว่าโค้ดกับหน้าเว็บต่อสายกันถูก:
  - JS ไปหา element ที่ไม่มีอยู่จริงหรือเปล่า (พิมพ์ id ผิด/ลบ HTML แต่ลืมลบโค้ด)
  - id ซ้ำในหน้าเดียวกันหรือเปล่า (getElementById เจอแค่ตัวแรก)
  - **ปุ่มที่วาดไว้มีคนรับคลิกครบทุกตัวไหม** (บั๊กปุ่มคัดลอกรอบก่อนคือแบบนี้)

### ตรวจบนเว็บจริง (production) — ผ่านหมด
- [x] เปิดครบทั้ง 7 หน้า ไม่มีข้อผิดพลาดใน console เลยสักหน้า
- [x] จอ 390px และ **จอแคบสุด 320px** ไม่มีหน้าไหนเลื่อนซ้ายขวาได้ (กว้าง 305px พอดีทุกหน้า)
- [x] หน้าเติมเงินสร้าง QR พร้อมเพย์ได้จริงหลังใส่ลายเซ็นไฟล์ (integrity) เข้าไป

### ตรวจแล้วไม่พบปัญหา
- กฎความปลอดภัย (`firestore.rules`) อ่านซ้ำทั้งไฟล์ — ทุกคอลเลกชันปฏิเสธเป็นค่าเริ่มต้น
  และคำสั่งดึงรายการของลูกค้าถูกบังคับให้กรอง uid ตั้งแต่ต้นทุกจุด
- เส้นทางรับซองอั่งเปาใน worker — การจองรหัสซองกันใช้ซ้ำ/ยิงพร้อมกัน ยังรัดกุมดี
- ไม่มีเครื่องหมาย TODO/FIXME ค้างในโค้ด

รวมทั้งเว็บ 1128 ข้อ ผ่านหมด

---
## รอบรีวิวโค้ดตัวเอง #4 (6 ก.ย. 2569) — แก้แล้ว
> รอบนี้ไล่: หน้าเข้าสู่ระบบ · กราฟหลังบ้าน · ตัวจัดการภาษา (i18n)

- [x] **ช่องโหว่ลิงก์พาออกนอกเว็บที่หน้าเข้าสู่ระบบ (open redirect)**
  เดิมเอาค่าจาก `?next=` ไปสั่งเปลี่ยนหน้าตรงๆ คนร้ายส่งลิงก์
  `login.html?next=https://เว็บปลอม` ให้ลูกค้าได้ พอล็อกอินเสร็จจะถูกพาไปเว็บปลอม
  ที่ทำหน้าตาเหมือนของเราแล้วหลอกเอารหัสผ่านซ้ำ
  → รับเฉพาะ 4 หน้าในเว็บเราเท่านั้น (index/wallet/purchases/admin)
- [x] **เบราว์เซอร์ที่ปิดการเก็บข้อมูลเว็บ เปิดเว็บไม่ขึ้นทั้งเว็บ**
  โหมดส่วนตัวบางตัว/ตั้งค่าบล็อกคุกกี้ จะ "โยนข้อผิดพลาด" ตอนแตะ localStorage
  `getLang()` ใน i18n.js ถูกเรียกจากทุกข้อความในเว็บ แต่ไม่ได้ดักไว้เลย
  → ดักแล้ว + จำภาษาไว้ในหน้าปัจจุบันแทน (สลับภาษายังใช้ได้ แค่ไม่จำข้ามหน้า)
- [x] **สั่งซื้อสำเร็จแล้วแต่ล้างตะกร้าไม่ได้ = ลูกค้าเห็นว่า "สั่งซื้อไม่สำเร็จ"**
  ทั้งที่ตัดเครดิตและสร้างออเดอร์ไปเรียบร้อยแล้ว แล้วจะกดสั่งซ้ำ = เสียเงินสองรอบ
  → ขั้นตอนเก็บกวาดหลังสั่งซื้อสำเร็จ ห้ามพาไปเข้าทางข้อผิดพลาดอีก
- [x] **กราฟสินค้าขายดีล่มถ้ามีออเดอร์ที่ไม่มีชื่อสินค้า** (ข้อมูลเก่า/แก้มือ)
  ทำให้หน้าภาพรวมทั้งหน้าไม่ขึ้น → แปลงเป็นข้อความก่อนเสมอ
- [x] กราฟเส้น: กันกรณีคำนวณตำแหน่งไม่ได้ (กราฟที่ยังไม่มีความกว้างจริง) ตอนเลื่อนเมาส์

### เทสที่เพิ่ม
- [x] `?next=` ต้องพาไปได้เฉพาะหน้าในเว็บเรา (ทดสอบลิงก์นอก 6 แบบ รวม javascript: และ //)
- [x] i18n ต้องทำงานได้แม้ localStorage โยนข้อผิดพลาดทุกครั้ง
- [x] สั่งซื้อสำเร็จแล้วล้างตะกร้าไม่ได้ ต้องยังบอกว่าสำเร็จ + ออเดอร์ต้องถูกสร้างจริง
- [x] กราฟรับข้อมูลผิดปกติได้ (ไม่มีชื่อ/null/ตัวเลข/ชื่อยาวมาก)

รวมทั้งเว็บ 1154 ข้อ ผ่านหมด

---
## รอบรีวิวโค้ดตัวเอง #5 (6 ก.ย. 2569) — แก้แล้ว
> รอบนี้ไล่: หน้าเงื่อนไข/นโยบาย · ไฟล์ตั้งค่า · auth.js ทั้งไฟล์ · ความตรงกันของกฎจริงกับกฎจำลอง

- [x] **เซสชันหมดอายุแล้วกดสั่งซื้อ ขึ้นข้อความภาษาโปรแกรม** — ลูกค้าเปิดหน้าค้างไว้นาน
  แล้วกดสั่งซื้อ จะเจอข้อความอ่านไม่รู้เรื่องแทนที่จะบอกให้เข้าสู่ระบบใหม่
- [x] **ลิงก์ติดต่อในหน้าเงื่อนไข/นโยบาย ไม่ได้กรองรูปแบบ** — ถ้าตั้งค่าผิดพลาด
  (พิมพ์ตกหล่น/ใส่ javascript:) จะได้ลิงก์เสียค้างอยู่ในหน้า → กรองเหมือนที่ contact.js ทำ
- [x] **ซ่อมข้อมูลคลังสินค้าที่พังเกิน 500 ชิ้นพร้อมกัน จะล้มทั้งคำสั่ง**
  (Firestore เขียนได้ 500 รายการต่อครั้ง ที่อื่นแบ่งชุดไว้แล้ว เหลือจุดนี้จุดเดียว)
- [x] คำแปล `o_UNAUTHORIZED` ถูกประกาศซ้ำ — **เทส t26 ที่เพิ่มไว้รอบแรกจับได้ทันทีที่เผลอเพิ่มซ้ำ**

### เพิ่มเครื่องมือกันพลาด
- [x] `tests/t28-rules-sync.mjs` — บังคับให้ **กฎจริง (firestore.rules) กับกฎจำลองในเทส ตรงกันเสมอ**
  ตรวจว่า: คอลเลกชันครบทุกตัว · คำสั่งที่ปิดตายในกฎจริงต้องปิดในกฎจำลองด้วย ·
  credit/role แก้จากเบราว์เซอร์ไม่ได้ทั้งสองที่ · สิทธิ์แอดมินยังใช้ 2 ชั้น · ไม่มีอีเมลฝังในกฎแล้ว
  *(สำคัญ: ถ้ากฎจำลองล้าสมัย เทสทั้งชุดจะ "ผ่านหมด" ทั้งที่ของจริงอาจเปิดช่องไว้)*

รวมทั้งเว็บ 1185 ข้อ ผ่านหมด

---
## รอบรีวิวโค้ดตัวเอง #6 (6 ก.ย. 2569) — แก้แล้ว
> รอบนี้: ยิงเคสแปลกใส่เซิร์ฟเวอร์จริง + ไล่ worker ทีละบรรทัดอีกรอบ

### ยิงคำขอรูปแบบแปลกๆ ใส่เซิร์ฟเวอร์จริง 24 เคส — ผ่านหมด
- items เป็น null/ข้อความ/ตัวเลข · qty เป็นวัตถุ · id เป็นอาร์เรย์ · id มีเครื่องหมายทับ (พยายามออกนอกที่เก็บ)
- ยัด `__proto__` · ยัดฟิลด์ราคา/ยอดรวม/สถานะเข้ามาเอง · body ไม่ใช่ JSON · idToken เป็นวัตถุ/อาร์เรย์
- ลูกค้ายิงเส้นทางแอดมินทุกเส้น · โดเมนแปลกยิงเข้ามา
- **ข้อความผิดพลาดไม่หลุดรายละเอียดภายในเลย** (ไม่มีชื่อ service account / เส้นทางในระบบ / stack trace)

### แก้ในโค้ด
- [x] **จำนวนสินค้าที่เป็นค่าคำนวณไม่ได้ ทำให้สต๊อกกลายเป็นค่าเสียถาวร**
  ออเดอร์เก่า/ข้อมูลที่ถูกแก้มือ ถ้า qty ไม่ใช่ตัวเลข ด่านเช็คสต๊อกจะผ่านหมด
  (ค่าที่คำนวณไม่ได้เทียบกับอะไรก็ false) แล้วเขียนสต๊อกเป็นค่าเสีย
  ผลคือ **สินค้านั้นขายได้ไม่จำกัด** → ตรวจ qty ทุกชิ้นก่อนเสมอ (1-999)
- [x] ยอดรวมออเดอร์ที่เป็น 0 หรือติดลบ อนุมัติไม่ได้อีกแล้ว
- [x] เพิ่มด่านตรวจยอดเงินทุกจุดที่อ่านจากฐานข้อมูล (safeMoney)

### เทสที่เพิ่ม
- [x] เอกสารที่ข้อมูลเสีย (ยอดรวม/จำนวน/เครดิต) ต้องไม่ลามไปทำเครดิตหรือสต๊อกพัง
  และเครดิตที่เสียต้องถูกซ่อมกลับเป็นตัวเลขที่ใช้งานได้

รวมทั้งเว็บ 1193 ข้อ ผ่านหมด

---
## รอบรีวิวโค้ดตัวเอง #7 (6 ก.ย. 2569) — แก้แล้ว
> รอบนี้: ความสมจริงของตัวจำลองในเทส · ฟอร์มสินค้า/คลังในหลังบ้าน · การเข้าถึงของหน้าเว็บ

### แก้เรื่องการใช้งานสำหรับคนใช้โปรแกรมอ่านหน้าจอ
- [x] **ป้ายกำกับช่องกรอกไม่ได้ผูกกับช่อง 17 จุด** (หน้าเข้าสู่ระบบ 6 · เติมเงิน 3 · หลังบ้าน 8)
  โปรแกรมอ่านหน้าจอจะอ่านว่า "ช่องว่าง" แทนที่จะบอกว่าเป็นช่องอีเมล/รหัสผ่าน/จำนวนเครดิต
- [x] ช่องใส่อีโมจิแทนรูปสินค้า ไม่มีคำอธิบายเลย (มีแต่ placeholder เป็นรูปอีโมจิ)

### ทำให้เทสสมจริงขึ้น (เทสที่หลอกตัวเอง = อันตรายกว่าไม่มีเทส)
- [x] **Firestore จำลองเคยรับค่า `undefined`** แต่ของจริงปฏิเสธ — ตอนนี้ปฏิเสธเหมือนกัน
  (ตรวจแล้วโค้ดปัจจุบันไม่มีจุดไหนส่ง undefined เลย แต่กันไว้สำหรับของใหม่)
- [x] **โค้ดสำรองตอนยังไม่มี index ของ Firestore ไม่เคยถูกทดสอบเลย**
  เป็นโค้ดที่ทำให้ประวัติการซื้อยังเปิดดูได้ตอน index เพิ่ง deploy หรือถูกลบ
  เพิ่มการจำลองสถานการณ์นั้น + เทสยืนยันว่าประวัติยังครบและเรียงถูกต้อง

### ตรวจแล้วไม่พบปัญหา
- ตัวจำลอง Firestore ตรงกับของจริงในเรื่องสำคัญ: กฎถูกตรวจกับ "คำสั่ง" ไม่ใช่ผลลัพธ์ ·
  `orderBy` ตัดเอกสารที่ไม่มีฟิลด์นั้นทิ้ง (กฎเหล็กข้อ 5) · แก้เอกสารที่ไม่มีอยู่แล้วต้องพัง ·
  transaction/batch ย้อนกลับได้เมื่อผิดพลาด
- ฟอร์มสินค้า/คลังสินค้าในหลังบ้าน · ตัวช่วยรีเฟรชเฉพาะแถว — เขียนรัดกุมดี ไม่พบปัญหา

รวมทั้งเว็บ 1205 ข้อ ผ่านหมด

---
## รอบรีวิวโค้ดตัวเอง #8 (6 ก.ย. 2569) — แก้แล้ว
> รอบนี้: style.css ทั้งไฟล์ · โหมดมืด · ดูของจริงในเบราว์เซอร์

- [x] **โหมดมืดเป็นโค้ดที่ไม่มีวันทำงาน** — CSS มีชุดสีโหมดมืดครบทั้งชุด
  แต่ไม่มีอะไรตั้งค่า `data-theme` เลยสักที่ และไม่ได้อ่านค่าจากเครื่องผู้ใช้
  แปลว่าผู้ใช้ไม่มีทางเห็นโหมดมืดเลย ทั้งที่เช็คลิสต์เขียนว่า "รองรับแล้ว"
  → ใส่สคริปต์สั้นๆ ใน `<head>` ทุกหน้า อ่านค่าจากเครื่องแล้วตั้งให้ (วางก่อนไฟล์สไตล์ จะได้ไม่กระพริบ)
  → `ui.js` คอยฟังกรณีผู้ใช้สลับโหมดของเครื่องระหว่างเปิดหน้าค้างไว้
  → **ดูของจริงในเบราว์เซอร์แล้ว** ทั้งหน้าร้านและหลังบ้าน (กราฟ/ตาราง/การ์ด อ่านออกครบ)
- [x] เทส t12 พังเพราะสคริปต์ที่เพิ่งเพิ่ม (ตัวดึงสคริปต์ในเทสจับผิดก้อน) — แก้ให้จับก้อนสุดท้ายก่อน `</body>`

### เทสที่เพิ่ม
- [x] `tests/t29-dark-mode.mjs` — ทุกหน้าต้องมีสคริปต์ตั้งโหมดมืด และต้องอยู่ก่อนไฟล์สไตล์ ·
  รันสคริปต์จริงทั้งกรณีเครื่องตั้งมืด/สว่าง/เบราว์เซอร์เก่าที่ไม่รองรับ ·
  ชุดสีโหมดมืดต้องครบทุกสีหลัก

### ตรวจแล้วไม่พบปัญหา
- CSS ไม่มีกฎที่ไม่มีวันถูกใช้ (ที่ดูเหมือนไม่ถูกใช้ คือคลาสที่ประกอบชื่อจาก JS
  หรือของฟีเจอร์ที่พักไว้ตั้งใจ เช่นปุ่มเฟซบุ๊กที่รอ Meta อนุมัติ)

รวมทั้งเว็บ 1229 ข้อ ผ่านหมด

---
## รอบรีวิวโค้ดตัวเอง #9 (6 ก.ย. 2569) — แก้แล้ว
> รอบนี้: ลองกดใช้งานจริงบนขนาดจอมือถือ · สไตล์สำหรับการพิมพ์ · ไล่เส้นทางซองอั่งเปาอีกรอบ

- [x] **ปุ่มคัดลอกเล็กเกินกดด้วยนิ้ว (24x20px)** — อยู่ในหน้าเติมเงิน (คัดลอกเลขบัญชี/เบอร์)
  และหน้าประวัติการซื้อ (คัดลอกไอดี/รหัสผ่านที่ซื้อมา) ซึ่งเป็นปุ่มที่ลูกค้าใช้บ่อยที่สุดบนมือถือ
  → ขยายพื้นที่กดเป็น 40x40px (ขนาดที่เห็นเท่าเดิม แค่กดโดนง่ายขึ้น)
- [x] **ไม่มีสไตล์สำหรับการพิมพ์** — ลูกค้าที่พิมพ์ประวัติการซื้อเก็บเป็นหลักฐาน
  จะได้กระดาษพื้นดำ (ถ้าใช้โหมดมืด) และมีปุ่มลอยทับเนื้อหา
  → เพิ่ม `@media print`: บังคับพื้นขาวตัวอักษรดำ ซ่อนหัวเว็บ/ปุ่มลอย/ปุ่มกด ไม่ตัดกลางการ์ด

### ทดสอบใช้งานจริงบนจอ 390px — ผ่านหมด
- กดเพิ่มลงตะกร้าจริง ตัวเลขบนไอคอนขึ้นถูกต้อง · เปิดตะกร้าแล้วกล่องอยู่ในจอพอดี ·
  ปุ่มสั่งซื้ออยู่ในจอและสูงพอกดด้วยนิ้ว · ไม่มีหน้าไหนเลื่อนซ้ายขวาได้

### ตรวจแล้วไม่พบปัญหา
- เส้นทางรับซองอั่งเปา (อ่านซ้ำอีกรอบ): การจองรหัสซอง · เงื่อนไข "ต้องยังไม่มีใครแตะเอกสาร"
  ก่อนปิดรายการ · กรณีแอดมินอนุมัติตัดหน้าบอท — **ยังถูกต้องหลังย้ายงานแอดมินไปที่เซิร์ฟเวอร์แล้ว**
  (แอดมินอนุมัติผ่าน /admin/topup/approve ตอนบอทกำลังทำอยู่ = บอทจะรู้แล้วไม่เติมซ้ำ)
- ปุ่มอื่นๆ บนมือถือ (36-38px) อยู่ในเกณฑ์ใช้งานได้ ไม่ต้องแก้

### เทสที่เพิ่ม
- [x] ปุ่มคัดลอกต้องมีพื้นที่กดอย่างน้อย 40px (กันแก้ CSS แล้วเล็กลงอีก)

รวมทั้งเว็บ 1230 ข้อ ผ่านหมด

---
## รอบรีวิวโค้ดตัวเอง #10 (6 ก.ย. 2569) — แก้แล้ว
> รอบนี้: **รีวิวตัวเทสเอง** (เทสมีบั๊กไหม / มองข้ามอะไรอยู่) · index ของฐานข้อมูล ·
> ไฟล์ประกอบเว็บ (robots/sitemap/firebase.json) · ความตรงของเอกสาร CLAUDE.md

### บั๊กในโค้ดเว็บ
- [x] **ปุ่มคัดลอกค้างเป็น ✓ ถาวรถ้ากดรัวๆ** — กดครั้งที่สองภายใน 1.2 วินาที โค้ดจะจำ "✓"
  เป็นป้ายเดิมของปุ่ม พอหมดเวลาจึงคืนค่าเป็น "✓" ตลอดไป ลูกค้าไม่รู้ว่ากดคัดลอกได้อีก
  → เก็บป้ายเดิมไว้ครั้งแรกครั้งเดียว + ยกเลิกตัวจับเวลาเดิมก่อนตั้งใหม่ (`ui.js`)
- [x] **ปุ่มคัดลอกที่ไม่มีอะไรให้คัดลอก** — หน้าประวัติการซื้อวาดปุ่มคัดลอกเสมอ
  ต่อให้ไอดี/รหัสผ่านของชิ้นนั้นว่าง (แสดงเป็น "—") กดแล้วไม่เกิดอะไรขึ้นเลย ไม่มีข้อความบอก
  → วาดปุ่มเฉพาะเมื่อมีค่าจริง (`purchases.js`) เหมือนที่หน้าเติมเงิน/หลังบ้านทำอยู่แล้ว
- [x] **เบราว์เซอร์กับเซิร์ฟเวอร์อ่านลิงก์ซองอั่งเปาไม่เหมือนกัน** — ฝั่งเบราว์เซอร์ไม่ตัดช่องว่าง
  หัวท้าย และไม่จำกัดความยาวรหัส (เซิร์ฟเวอร์จำกัด 64 ตัว) ลูกค้าที่วางลิงก์ติดช่องว่างมา
  จะกดส่งได้ แต่เซิร์ฟเวอร์ตอบว่าลิงก์ไม่ถูกต้องโดยไม่บอกสาเหตุ
  → ทำให้ `parseAngpaoCode()` ใน `shop-config.js` ตรงกับของ worker ทุกบรรทัด

### บั๊กในตัวเทสเอง (เทสที่เชื่อถือไม่ได้ = อันตรายกว่าไม่มีเทส)
- [x] **ตัวรันเทสรายงานว่า "ผ่าน" ทั้งที่ไฟล์เทสล้ม** — ถ้าเทสพิมพ์บรรทัดสรุปแล้วค่อยพัง
  (unhandled rejection / throw ตอนปิดโปรแกรม) ตัวรันจะดูแค่บรรทัดสรุปแล้วนับว่าผ่าน
  → ดูรหัสออกของ node ด้วย ถ้าไม่ใช่ 0 ทั้งที่สรุปว่าผ่านหมด = นับเป็นไม่ผ่าน + พิมพ์ท้าย log ให้ดู
- [x] **ตัวเตรียมโค้ดสำหรับเทสข้ามไฟล์ที่ชื่อมีตัวเลข** — `harness.mjs` แปลงชื่อไฟล์ที่ import
  ด้วยรูปแบบ `[a-z-]+` เท่านั้น ไฟล์อย่าง `otp2.js` จะไม่ถูกแปลงและโหลดไม่ขึ้นแบบเงียบๆ
  → รับตัวเลขด้วย + ตรวจซ้ำหลังเตรียมเสร็จ ถ้ายังเหลือ import ที่ชี้ไป `.js` ให้หยุดแล้วบอกทันที
- [x] **`increment()` ปลอมเก็บค่าผิด** — Firestore จำลองเก็บก้อน `{ __inc: 1 }` ลงเอกสารตรงๆ
  แทนที่จะบวกจากค่าเดิม ถ้าวันหลังมีใครใช้ เทสจะเขียวทั้งที่ข้อมูลจริงเสีย
  → ทำให้บวกจริงเหมือนของจริง และฟ้องทันทีถ้าเอาไปใช้กับฟิลด์ที่ไม่ใช่ตัวเลข

### เทสที่เพิ่ม
- [x] `tests/t30-copy-button.mjs` (17 ข้อ) — ปุ่มคัดลอกทำงานจริง: เข้าคลิปบอร์ด · กดรัวๆ ป้ายไม่ค้าง ·
  เบราว์เซอร์ที่ไม่มี `navigator.clipboard` ต้องถอยไปใช้วิธีสำรอง · คัดลอกไม่ได้ต้องขึ้น ✕ ไม่ใช่เงียบ ·
  คลิกไอคอนข้างในปุ่มก็ต้องติด · และห้ามวาดปุ่มที่ไม่มีค่าให้คัดลอก
- [x] `tests/t31-cross-os.mjs` (10 ข้อ) — **เว็บต้องเปิดได้เหมือนกันทุกระบบปฏิบัติการ**:
  ทุกไฟล์ที่ HTML/CSS/JS เรียกใช้ต้องมีจริงและตัวพิมพ์ตรงเป๊ะ (Windows ไม่สน แต่ Linux
  ที่ GitHub Pages ใช้จริงสนมาก) · ห้ามมีไฟล์ที่ต่างกันแค่ตัวพิมพ์ · ห้ามใช้ชื่อไฟล์ต้องห้ามของ Windows ·
  ไฟล์ข้อความต้องเป็น LF ไม่มี BOM · ห้ามมีพาธแบบ `C:\` ในโค้ดเว็บ
- [x] เทียบ `parseAngpaoCode()` ฝั่งเบราว์เซอร์กับฝั่งเซิร์ฟเวอร์ 13 รูปแบบลิงก์ ต้องได้ผลตรงกันทุกอัน
  (เพิ่มใน `t4-angpao.mjs`)

### ตรวจแล้วไม่พบปัญหา
- `firestore.indexes.json` — คำสั่งที่มีทั้ง `where` และ `orderBy` มีแค่ 2 อัน (ประวัติออเดอร์
  และประวัติเติมเงินของตัวเอง) และมี index ครบทั้งคู่ · ที่เหลือเป็น `orderBy` เดี่ยวซึ่ง Firestore
  สร้าง index ให้เองอยู่แล้ว
- `firebase.json` / `robots.txt` / `sitemap.xml` — หน้าสาธารณะอยู่ในแผนผังครบ หน้าส่วนตัว
  (เติมเงิน/ประวัติ/หลังบ้าน) ถูกกันไว้ทั้งใน robots และ meta noindex
- `CLAUDE.md` — อ้างถึงไฟล์ 24 ไฟล์ มีอยู่จริงทุกไฟล์ ไม่มีของเก่าค้าง (ADMIN_EMAILS /
  ownerEmails / runTransaction หายไปหมดแล้ว)
- กฎจำลองใน `fake/store.mjs` เทียบกับ `firestore.rules` ทีละบรรทัดอีกรอบ — ตรงกันทุกข้อ

### เอกสารที่อัปเดต
- `tests/README.md` — ตารางรายชื่อเทสหยุดอยู่แค่ t19 มา 12 ไฟล์แล้ว → เติมให้ครบถึง t31
- `CLAUDE.md` — เพิ่มกฎ: หน้าใหม่ต้องคัดลอกสคริปต์โหมดมืดไปวางก่อน `style.css` และต้องอัปเดต
  `lastmod` ใน sitemap · บันทึกว่า `ui.js` เป็นเจ้าของปุ่มคัดลอกทุกปุ่มแล้ว ·
  บันทึกว่าตัวอ่านสลิปเป็นแค่ตัวช่วยอ่าน ไม่ใช่การยืนยันว่าสลิปจริง
- `sitemap.xml` — `lastmod` ทุกหน้าเป็น 2026-09-06

รวมทั้งเว็บ 1260 ข้อ ผ่านหมด

---
## รอบ "ซื้อทันที ไม่ต้องรออนุมัติ" (6 ก.ย. 2569) — ทำแล้วทั้งเรื่อง 1+2+3 + เงื่อนไขเคลม

### สิ่งที่เปลี่ยนไปสำหรับลูกค้า
- **ไม่มีตะกร้าแล้ว** กด "สั่งซื้อ" ที่การ์ดสินค้า → เปิดหน้าต่างของชิ้นนั้นเลย
  เลือกจำนวนได้ (ปุ่ม +/− หรือพิมพ์เอง) · กรอกไอดีเกมถ้าสินค้านั้นขอ ·
  **ต้องติ๊กยอมรับเงื่อนไขการเคลมก่อน** ถึงจะกดยืนยันได้
- **ไอดีเกม** — กดยืนยันแล้วได้รหัสทันที ขึ้นในประวัติการซื้อเลย ไม่ต้องรอแอดมิน
- **ของเติมเกม** — หักเครดิตทันทีเหมือนกัน แล้วรอแอดมินเติมให้
  สถานะไล่จาก **รอดำเนินการ → กำลังดำเนินการ → สำเร็จ**
- **แก้ไอดีเกมที่กรอกผิดได้เอง** ในประวัติการซื้อ แต่เฉพาะตอน "รอดำเนินการ"
  พอแอดมินกดเริ่มดำเนินการ ปุ่มแก้ไขหายทันที (มีคำเตือนบอกไว้ในกล่อง)
- **นับถอยหลังเวลาแจ้งเคลม 10 นาที** โชว์ในประวัติการซื้อ หมดเวลาแล้วขึ้นว่า "หมดเวลาแจ้งเคลมแล้ว"

### สิ่งที่เปลี่ยนไปสำหรับแอดมิน
- แท็บออเดอร์เปลี่ยนตัวกรองเป็น **รอดำเนินการ / สำเร็จ / ยกเลิกแล้ว / ทั้งหมด**
- ปุ่มใหม่: **เริ่มดำเนินการ** · **ทำเสร็จแล้ว** · **ยกเลิก + คืนเครดิต**
- กด "ทำเสร็จแล้ว" ทีเดียวได้ 2 อย่าง: เริ่มจับเวลาเคลมให้ลูกค้า + **ลบชื่อผู้ใช้/รหัสผ่านลูกค้าอัตโนมัติ**
- ยกเลิกได้ทั้งตอนยังไม่เริ่ม กำลังทำ และหลังส่งของแล้ว (เคสลูกค้าเคลมทัน)
  คืนเครดิตเต็มจำนวน + คืนสต๊อกเฉพาะของที่ยังไม่ได้ส่งมอบ
- เห็น **ประวัติที่ลูกค้าแก้ข้อมูลเอง** (เวลา + ค่าเดิม) ในออเดอร์ กันเถียงกันทีหลัง

### ออเดอร์เก่ายังใช้ปุ่มชุดเดิม (สำคัญมาก)
ออเดอร์ที่ค้างอยู่ก่อนเปลี่ยนระบบ **ยังไม่ถูกหักเครดิต** ถ้าเอาปุ่มใหม่ไปกดจะได้ของฟรี
จึงแยกด้วยฟิลด์ `paid`:
- ไม่มี `paid` = ออเดอร์เก่า → ปุ่ม "อนุมัติ / ไม่อนุมัติ" เหมือนเดิม + ป้ายบอกว่า "ออเดอร์ระบบเก่า"
- `paid: true` = ออเดอร์ใหม่ → ปุ่มชุดใหม่
เซิร์ฟเวอร์กันไว้อีกชั้น (ยิงผิดชุดตอบ `NEW_FLOW_ORDER` / `OLD_ORDER`)

### กฎความปลอดภัยไม่ต้องแก้เลย
กฎ `orders` ยังปิดสนิท `allow create, update, delete: if false` เหมือนเดิม
ลูกค้าแก้ไอดีเกมผ่าน Worker `/order/edit-info` แทน — **ห้ามเปลี่ยนไปเปิดกฎให้เขียน items เอง**
เพราะ items เป็นอาร์เรย์ กฎ Firestore ตรวจทีละสมาชิกไม่ได้ = เปิดช่องให้แก้ qty/price ไปด้วย

### เส้นทางเซิร์ฟเวอร์ที่เพิ่ม
| เส้นทาง | ใคร | ทำอะไร |
|---|---|---|
| `/order` | ลูกค้า | หักเครดิต + จ่ายของ + สร้างออเดอร์ **ใน transaction เดียว** |
| `/order/edit-info` | ลูกค้า | แก้ไอดีเกมในออเดอร์ตัวเอง (เฉพาะตอน pending) |
| `/admin/order/start` | แอดมิน | รอดำเนินการ → กำลังดำเนินการ |
| `/admin/order/complete` | แอดมิน | กำลังดำเนินการ → สำเร็จ + เริ่มจับเวลาเคลม + ลบรหัสผ่านลูกค้า |
| `/admin/order/cancel` | แอดมิน | ยกเลิก + คืนเครดิต + คืนสต๊อกที่ยังไม่ได้ส่ง |

### เทสที่เพิ่ม (รวมทั้งเว็บ 1415 ข้อ)
- `t3-worker` +39 ข้อ — 3 สถานะ · กดข้ามขั้นไม่ได้ · กดซ้ำไม่คืนเครดิตซ้ำ ·
  ยกเลิกออเดอร์ที่ส่งของแล้วต้องไม่คืนไอดีเข้าคลัง · ลูกค้าแก้ข้อมูลเอง (แก้ของคนอื่น/แก้ราคา/แก้หลังล็อก)
- `t5-shop` เขียนใหม่ทั้งไฟล์ 76 ข้อ — หน้าต่างสั่งซื้อทันที ทุกเคส
- `t6-purchases` +14 ข้อ — สถานะใหม่ · นับถอยหลังเวลาเคลม · แก้ข้อมูลเองในประวัติ
- `t7-admin-ui` +23 ข้อ — ปุ่มคนละชุดระหว่างออเดอร์เก่า/ใหม่ · ยกเลิกคืนเครดิต · ประวัติการแก้
- `t20-customer-info` เขียนใหม่ — ข้อมูลลูกค้าไปถึงออเดอร์ + ลบอัตโนมัติตอนกดเสร็จ
- `t24-admin-security` +18 ข้อ — คนอื่นยิง start/complete/cancel/edit-info ไม่ได้
- `t19-mobile` +11 ข้อ — กล่องสั่งซื้อบนจอแคบ (ปุ่ม 44px · ไม่ล้นจอ · ตัดคำได้)
- `t23-terms` +6 ข้อ — เงื่อนไขวิดีโอ + นับเป็นนาที

### ตรวจบนเบราว์เซอร์จริง (Chrome, 320 / 390 / 430 px + จอ PC) — ผ่านหมด
- หน้าร้าน + หน้าต่างสั่งซื้อ · ประวัติการซื้อ · หลังบ้าน — ไม่มีหน้าไหนเลื่อนซ้ายขวาได้
- ปุ่ม +/− และช่องจำนวนสูง 44px บนมือถือ · ปุ่มยืนยันเต็มความกว้าง
- แถบตัวกรองสถานะเฟดขอบขวาบอกว่าเลื่อนต่อได้ (เพิ่มใหม่รอบนี้)

### ยังไม่ได้ทำในรอบนี้ (ตั้งใจ)
- ~~นโยบายคืนเครดิตที่ยังไม่ใช้ ยังเขียนว่า "พิจารณาเป็นรายกรณี"~~ **ตัดสินใจแล้ว + แก้แล้ว (6 ก.ย. 2569)**
  → เติมแล้วถอน/แลกคืนเป็นเงินไม่ได้ทุกกรณี
- ออเดอร์ที่ยกเลิกแล้ว ลูกค้ายังเห็นรหัสที่เคยได้รับอยู่ (ตั้งใจ เก็บเป็นหลักฐานว่าเคยส่งอะไรไป)

---
## รอบเครื่องมือแอดมิน (6 ก.ย. 2569) — ทำแล้ว 4 เรื่อง
> ค้นหาออเดอร์ · ประวัติรายคน · แท็บบันทึกแอดมิน · ซ่อนรายการจากหน้าประวัติลูกค้า

### 1. ค้นหาออเดอร์ด้วยเลขที่คำสั่งซื้อ
- ช่องค้นหาอยู่เหนือตารางออเดอร์ · ตารางมีคอลัมน์ "เลขที่คำสั่งซื้อ" พร้อมปุ่มคัดลอกแล้ว
- เลขที่ = 8 ตัวแรกของรหัสออเดอร์แบบตัวใหญ่ **ตรงกับที่ลูกค้าเห็นในหน้าประวัติเป๊ะ**
- ใส่ `#` นำหน้า / ตัวเล็ก / มีช่องว่างติดมา ก็ยังหาเจอ (ระบบตัดให้เอง)
- ค้นด้วยชื่อลูกค้า อีเมล หรือชื่อสินค้า ก็ได้
- **ตอนค้นหาจะมองข้ามตัวกรองสถานะ** — ลูกค้าแจ้งเลขมาแล้วต้องเจอ ต่อให้ออเดอร์นั้นยกเลิกไปแล้ว
  (แถบตัวกรองจะหรี่ลงให้เห็นว่ากำลังถูกมองข้ามอยู่)

### 2. ประวัติของสมาชิกรายคน
- ปุ่ม "ประวัติ" ในทุกแถวของแท็บสมาชิก → เปิดกล่องที่มี ออเดอร์ + การเติมเงินของคนนั้น
- มีตัวเลขสรุป 3 ใบ: ยอดซื้อสำเร็จ / ยอดเติมที่อนุมัติแล้ว / เครดิตคงเหลือ
- ใช้ข้อมูลที่หน้าโหลดมาแล้ว **ไม่ยิงฐานข้อมูลเพิ่ม** (ประหยัดโควตาอ่านของ Firestore)
  ข้อแลกเปลี่ยน: เห็นได้เท่าที่อยู่ใน 500 รายการล่าสุดของทั้งร้าน — เขียนบอกไว้ท้ายกล่องแล้ว

### 3. แท็บบันทึกแอดมิน
- แท็บที่ 6 ในหลังบ้าน อ่านจาก `adminLogs` (เซิร์ฟเวอร์เขียนอย่างเดียว แก้/ลบไม่ได้เลย)
- โหลดตอนเปิดแท็บครั้งแรกเท่านั้น + มีปุ่ม "โหลดใหม่"
- แปลงรหัสสมาชิกเป็นชื่อคนให้อ่านง่าย · การปรับเครดิตโชว์ยอดก่อน → หลัง
- การกระทำที่ยังไม่มีคำแปล จะโชว์ชื่อดิบแทนที่จะขึ้นเป็น `act_xxx`

### 4. ซ่อนรายการจากหน้าประวัติของลูกค้า
- ปุ่ม "ซ่อนจากลูกค้า / เลิกซ่อน" ในแถวออเดอร์และแถวเติมเงิน
- เซิร์ฟเวอร์ตั้งฟิลด์ `hiddenAt` (เส้นทาง `/admin/order/hide` · `/admin/topup/hide`)
- **ไม่ลบข้อมูล ไม่ขยับเครดิต ยอดขายไม่เปลี่ยน** หลังบ้านยังเห็นทุกอย่างพร้อมป้าย "ซ่อนจากลูกค้าแล้ว"
- ทุกครั้งที่กด มีบันทึกลง `adminLogs`

#### ข้อจำกัดที่ต้องรู้ (สำคัญ)
เป็นการซ่อน **ที่หน้าจอ** ไม่ใช่การปิดสิทธิ์อ่าน — กฎ Firestore ยังให้เจ้าของอ่านเอกสารของตัวเองได้
ปิดในกฎไม่ได้จริง เพราะถ้าเขียนว่า "อ่านได้เมื่อ hiddenAt เป็น null" คำสั่งดึงประวัติต้องมี
`where("hiddenAt","==",null)` ตาม แล้วออเดอร์เก่าทุกใบที่ไม่มีฟิลด์นี้จะหายจากประวัติลูกค้าทั้งหมด
→ ใช้กับ "รายการที่รกหน้าจอ / ลูกค้าขอให้เอาออก" ไม่ใช่ "ความลับที่ห้ามลูกค้าเห็นเด็ดขาด"

### เทสที่เพิ่ม
- `tests/t32-admin-tools.mjs` — 53 ข้อ ครอบทั้ง 4 เรื่อง รวมถึง
  "สมาชิกธรรมดาอ่านบันทึกแอดมินไม่ได้" และ "สั่งซ่อนออเดอร์ไม่ได้"
- `tests/t24-admin-security.mjs` — เพิ่ม 4 ข้อ: ลูกค้ายิง `/admin/order/hide`,
  `/admin/topup/hide` ไม่ผ่าน · ออเดอร์ไม่ถูกซ่อน · แอดมินไล่อ่านบันทึกทั้งลิสต์ได้ (กฎ list คนละเส้นทางกับ get)
- รวมทั้งเว็บ **1473 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #12–13 (6 ก.ย. 2569) — แก้แล้ว
> รอบนี้: ให้เครื่องหาแทนตา (เทสเชิงกลไก) + ไล่เส้นทางเขียนของเซิร์ฟเวอร์อีกรอบ

### บั๊กจริงที่เจอ — เอกสารที่ถูกลบไปแล้วฟื้นกลับมาเป็นซาก
คำสั่ง update ของ Firestore **สร้างเอกสารให้ถ้ายังไม่มี** ไม่ได้แค่แก้ของเดิม
ถ้ามีใครลบออเดอร์/สมาชิกทิ้งหลังเซิร์ฟเวอร์อ่านค่าไปแล้ว จะได้เอกสารซากกลับเข้าฐานข้อมูล:
- `orders/<id>` ที่มีแต่ฟิลด์ `hiddenAt` ฟิลด์เดียว (ไม่มียอดเงิน ไม่มีเจ้าของ)
- `users/<uid>` ที่มีแต่ `role: admin` = **บัญชีเปล่าที่เป็นแอดมิน**

แก้โดยใส่เงื่อนไข `currentDocument: { exists: true }` ในทั้งสองที่
(ไม่ต้องใช้ transaction เพราะไม่มีการอ่าน-คำนวณ-เขียนที่ต้องล็อก แค่กันเคสนี้ก็พอ)

พิสูจน์แล้วว่าเทสจับได้จริง: ถอดโค้ดที่แก้ออก → เทส 2 ข้อล้มทันที

### เครื่องมือกันพลาดที่เพิ่ม (ให้เครื่องหาแทนตา)
- `t26-i18n` — ทุกคีย์ที่ JS เรียกใช้ตรงๆ ต้องมีคำแปลครบทั้งไทย/อังกฤษ
  *คีย์ที่พิมพ์ผิดไม่ทำให้เว็บพัง แต่จะโชว์ชื่อคีย์ดิบให้ลูกค้าเห็น — ตาคนมองข้ามง่ายที่สุด*
- `t27-dom-wiring` — ทุกค่าใน `data-act` ต้องมีคนรับไปทำงานจริง และในทางกลับกัน
  *พิมพ์ชื่อไม่ตรงกันสักตัว ปุ่มนั้นจะกดแล้วเงียบสนิท ไม่มี error ให้เห็น*
- `t3-worker` — Firestore จำลองบังคับเงื่อนไข `exists: true` แล้ว
  + ปุ่มจำลอง "มีคนลบเอกสารทิ้งทันทีหลังเราอ่าน"
- ทั้งสองตัวพิสูจน์แล้วว่าจับบั๊กที่แกล้งใส่เข้าไปได้จริง

### ตรวจบนเว็บจริง
ไฟล์บน GitHub Pages มีโค้ดใหม่ครบทุกไฟล์ (admin.html / admin.js / i18n.js / style.css)

รวมเทสทั้งเว็บ **1509 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #14 (6 ก.ย. 2569) — แก้แล้ว
> รอบนี้: ปัญหาที่รู้อยู่แล้วจากรอบก่อน — หน้าหลังบ้านโหลดออเดอร์มาแค่ 500 ใบล่าสุด

### ค้นออเดอร์เก่าที่หน้ายังไม่ได้โหลดมา
ลูกค้าแจ้งเลขที่คำสั่งซื้อของออเดอร์ที่เก่ากว่า 500 ใบล่าสุด → เดิมหาไม่เจอ
และแอดมินอาจสรุปผิดว่า "ไม่เคยสั่ง" ทั้งที่มีอยู่จริงในฐานข้อมูล

- ค้นไม่เจอในหน้า + คำค้นเป็นเลขที่คำสั่งซื้อ → โผล่ปุ่ม **"ค้นทั้งฐานข้อมูลด้วยเลขที่นี้"**
- ไม่ค้นอัตโนมัติ เพราะจะยิงฐานข้อมูลทุกครั้งที่พิมพ์ทีละตัวอักษร
- ค้นด้วยช่วงของรหัสเอกสาร (`documentId() >= p` และ `< p + อักขระท้ายสุด`)
  ใช้ได้เพราะรหัสออเดอร์ที่เซิร์ฟเวอร์สร้างเป็นเลขฐานสิบหกตัวเล็กล้วน
  เลขที่ 8 ตัวที่ลูกค้าเห็นจึงเป็น "ส่วนหน้าของรหัส" พอดี — **ไม่ต้องเพิ่มฟิลด์ ไม่ต้องสร้าง index**
- ค้นแล้วไม่มีจริง บอกชัดว่า "ค้นทั้งฐานข้อมูลแล้วไม่มีออเดอร์เลขที่นี้จริงๆ"
  ต่างจาก "ไม่พบใน 500 รายการล่าสุดที่โหลดมา"
- ค้นไม่สำเร็จ (เน็ตหลุด/ฐานข้อมูลปฏิเสธ) บอกว่าไม่สำเร็จ ไม่ใช่เงียบแล้วให้เข้าใจว่าไม่มี

### ที่เกือบพลาดเอง
ตอนแรกใส่ตัวจำว่า "เคยค้นคำนี้ไปแล้ว" เพื่อกันยิงซ้ำ — แต่พอแอดมินพิมพ์คำอื่นแล้วกลับมาคำเดิม
ปุ่มจะหายและผลก็หายไปด้วย = ออเดอร์ที่มีอยู่จริงกลายเป็นหาไม่เจอ **เอาออกแล้ว**
(ยิงซ้ำ 1 ครั้งถูกกว่าการทำให้แอดมินเข้าใจผิดว่าไม่มีออเดอร์)

### ยิงกับ Firestore ตัวจริงแล้ว — ผ่าน (รอบ 15)
ทดสอบบนเว็บจริงด้วยบัญชีแอดมิน 4 เคส ผ่านหมด ไม่มี error และไม่ต้องสร้าง index เพิ่ม:
- ช่วงที่ถูกต้อง (`>= เลขที่` และ `< เลขที่ + อักขระท้ายสุด`) → เจอออเดอร์ใบที่ต้องการพอดี 1 ใบ
- ช่วงว่างเปล่า → 0 ใบ (ไม่มีผลหลุดมาเกิน)
- เลขที่ที่ไม่มีจริง → 0 ใบ
- กวาดทั้งคอลเลกชัน → 11 ใบ (จำนวนออเดอร์ทั้งหมดในร้านตอนนี้)

รวมเทสทั้งเว็บ **1520 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #15 (6 ก.ย. 2569) — ตรวจบนเว็บจริง
> เปิดเว็บจริงในเบราว์เซอร์ ไม่ใช่แค่รันเทส — รอบนี้แก้ i18n.js กับ style.css ซึ่งกระทบทุกหน้า

### หน้าร้าน (index.html) — ปกติดี
- สินค้าแสดงครบ ไม่มี error ในคอนโซลเลย
- จอไม่เลื่อนซ้ายขวา
- คำแปลใหม่ครบทั้งไทยและอังกฤษ (ตรวจ 12 คีย์ ไม่มีตัวไหนโชว์เป็นชื่อคีย์ดิบ)

### หน้าหลังบ้าน — ของใหม่ขึ้นครบ
แท็บกลายเป็น 6 แท็บแล้ว: ภาพรวม / ออเดอร์ / เติมเงิน / สินค้า / สมาชิก / **บันทึกแอดมิน**
ช่องค้นหาออเดอร์ · ปุ่มค้นทั้งฐานข้อมูล · กล่องประวัติสมาชิก มีครบทุกตัว

### เรื่องที่ต้องรู้: เบราว์เซอร์เก็บไฟล์เก่าไว้ราว 10 นาที
ตอนตรวจเจอว่าเบราว์เซอร์ยังใช้ `i18n.js` กับ `auth.js` เวอร์ชันเก่าที่แคชไว้
ทำให้คำแปลใหม่ยังไม่ขึ้นทันที **ไม่ใช่บั๊ก** — GitHub Pages ตั้งอายุแคชไว้ประมาณ 10 นาที
หลังจากนั้นหายเอง ถ้าอยากเห็นทันทีให้กด Ctrl+F5 (หรือเปิดลิงก์ต่อท้ายด้วย ?1)

### ข้อสังเกตจากข้อมูลจริง — ผู้ใช้ตอบแล้ว (6 ก.ย. 2569)
ในร้านตอนนี้มีสินค้า 4 ชิ้นชื่อ **"สินค้าตัวอย่าง 1-4"** เปิดขายอยู่ ยังไม่รู้จะขายอะไรจริง
จึงใส่ไว้เป็นตัวอย่างก่อน — **ไม่ต้องแก้อะไร** ออเดอร์ที่มีอยู่เป็นเพื่อนผู้ใช้ทดลองซื้อเอง

---
## รอบรีวิวโค้ดตัวเอง #16 (6 ก.ย. 2569) — ยิงกฎจริงด้วยบัญชีสมาชิกธรรมดา
> กฎเหล็กของโปรเจกต์: ทดสอบด้วยบัญชีแอดมินจะซ่อนบั๊กเรื่องสิทธิ์ ต้องใช้บัญชีสมาชิกเท่านั้น

รัน `tests/live-rules-check.mjs` กับ Firestore ตัวจริง (สร้างบัญชีทดสอบชั่วคราว → ตรวจ → ลบทิ้ง)
**ผ่าน 38 ข้อ ไม่มีข้อไหนพลาด** และลบข้อมูลทดสอบออกครบแล้ว

### เพิ่มการตรวจ `adminLogs` เข้าไปในชุดยิงจริง (3 ข้อ)
แท็บบันทึกแอดมินอ่าน **ทั้งคอลเลกชัน (list)** ไม่ใช่ทีละใบ — กฎ list กับ get เป็นคนละเส้นทาง
และตัวจำลองกฎจับเคส list ไม่ได้ จึงต้องยิงจริงเท่านั้นถึงจะรู้ว่าปลอดภัย
- สมาชิกธรรมดาไล่อ่านบันทึกทั้งลิสต์ไม่ได้ (403)
- สมาชิกธรรมดาเปิดบันทึกทีละใบไม่ได้ (403)
- สมาชิกธรรมดาเขียนบันทึกปลอมไม่ได้ (403)

---
## รอบรีวิวโค้ดตัวเอง #17-18 (6 ก.ย. 2569) — แก้แล้ว

### บั๊กจริง: เครดิตลูกค้าโชว์เป็น ฿0 ทั้งที่แค่ยังไม่ได้โหลดข้อมูลมา
ตารางออเดอร์โชว์เครดิตคงเหลือของลูกค้าไว้ให้แอดมินดูประกอบการตัดสินใจ
แต่หน้านี้โหลดรายชื่อสมาชิกมาแค่ 500 คน — ถ้าหาไม่เจอ โค้ดเดิมแสดง **฿0**
แอดมินอ่านแล้วเข้าใจว่า "ลูกค้าคนนี้ไม่มีเครดิต" ทั้งที่อาจมีเต็มกระเป๋า
เห็นชัดที่สุดตอนใช้ปุ่มค้นออเดอร์เก่าจากฐานข้อมูล เพราะเจ้าของออเดอร์เก่ามักไม่อยู่ในรายชื่อที่โหลดมา
→ เปลี่ยนเป็นข้อความ **"ไม่ทราบ (ยังไม่ได้โหลดข้อมูลสมาชิกคนนี้)"** และลบโค้ดเก่าที่ไม่มีใครใช้แล้วออก

### โปรแกรมอ่านหน้าจอ
- ช่องค้นหาออเดอร์และช่องค้นหาสมาชิก มีป้ายกำกับผูกจริงแล้ว (เดิมมีแต่ข้อความจางในช่อง)
- บรรทัดบอกผลการค้นหาใส่ `aria-live` แล้ว — เดิมผลเปลี่ยนแล้วคนที่ใช้โปรแกรมอ่านหน้าจอไม่รู้เลย
- เพิ่มคลาส `.sr-only` (ป้ายที่ตาไม่เห็นแต่หูได้ยิน)

### สั่งพิมพ์
ช่องค้นหาไม่ต้องติดไปบนกระดาษแล้ว

### เครื่องมือกันพลาดที่เพิ่ม
- คลาสที่หน้าเว็บใช้ต้องมีสไตล์รองรับจริง (สไตล์หาย = หน้าเว็บดูผิดแบบเงียบๆ
  เช่น `.sr-only` ที่ไม่มีสไตล์ จะทำให้ป้ายสำหรับโปรแกรมอ่านหน้าจอโผล่มาบนจอ)
- บรรทัดที่เปลี่ยนข้อความเองต้องมี `aria-live`

---
## รอบรีวิวโค้ดตัวเอง #19 (6 ก.ย. 2569) — แก้แล้ว
> รอบนี้: ไล่ดูว่าเซิร์ฟเวอร์บันทึกอะไรไว้บ้าง แล้วหน้าเว็บแสดงครบไหม

เซิร์ฟเวอร์เขียนบันทึก 13 แบบ ตรวจทีละแบบว่ามีที่แสดงในแท็บบันทึกแอดมินหรือไม่
**เจอ 1 ฟิลด์ที่บันทึกไว้แต่ไม่มีใครแสดง**: `clearedInfo` — บอกว่าตอนแอดมินกด "ทำเสร็จแล้ว"
ระบบลบชื่อผู้ใช้/รหัสผ่านของลูกค้าทิ้งให้อัตโนมัติหรือเปล่า
เป็นข้อมูลที่ต้องตรวจย้อนหลังได้จริง (ลูกค้าถามว่ารหัสยังอยู่ในระบบไหม) → แสดงแล้ว

### เครื่องมือกันพลาดที่เพิ่ม
ทุกฟิลด์ที่ `auditWrite` เขียนลง adminLogs ต้องมีที่แสดงใน `logDetail` ของหน้าเว็บ
*เหตุผล: บันทึกที่ไม่มีใครแสดง = เท่ากับไม่ได้บันทึก ต้องไปเปิดฐานข้อมูลดูเอง*
พิสูจน์แล้วว่าจับได้จริง — ถอดการแสดง `clearedInfo` ออก เทสล้มทันที

รวมเทสทั้งเว็บ **1529 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #20 (6 ก.ย. 2569) — แก้แล้ว

### บั๊กจริง: กดปุ่มกับออเดอร์ที่ค้นเจอจากฐานข้อมูล แล้วยอดขายในหน้าภาพรวมกระโดด
ปุ่มทุกปุ่มในตารางออเดอร์ พอกดเสร็จจะดึงข้อมูลใบนั้นมาใหม่แล้ว **ยัดเข้ารายการที่หน้าโหลดมา**
ถ้าใบนั้นมาจากการ "ค้นทั้งฐานข้อมูล" (คือของเก่าที่หน้ายังไม่ได้โหลด) มันจะกลายเป็นส่วนหนึ่ง
ของก้อนข้อมูลที่การ์ดตัวเลขในหน้าภาพรวมเอาไปคิด → **ยอดขายเพิ่มขึ้นเองเงียบๆ ทั้งที่ไม่มีการขายเกิดขึ้น**

ทดสอบแล้วเห็นชัด: ซ่อนออเดอร์เก่า 900 บาทที่ค้นเจอ → ยอดขายทั้งหมดกระโดดจาก ฿300 เป็น ฿1,200
(หายเองเมื่อรีเฟรชหน้า แต่ระหว่างนั้นแอดมินอ่านตัวเลขผิด)

แก้โดยแยกเก็บ: แถวที่มาจากการค้นฐานข้อมูลอัปเดตในกองของตัวเอง ไม่ปนกับที่หน้าโหลดมา
พิสูจน์แล้วว่าเทสจับได้ — ถอดโค้ดที่แก้ออก เทสล้มทันที

รวมเทสทั้งเว็บ **1532 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #21 (6 ก.ย. 2569) — แก้แล้ว
> รอบนี้: ไล่แท็บบันทึกแอดมินที่เพิ่งทำ ในมุม "ใช้ตรวจย้อนหลังจริงได้ไหม"

### บั๊กจริง: บันทึกอ่านไม่รู้เรื่องพอดีกับตอนที่ต้องใช้มากที่สุด
บันทึกโชว์ชื่อลูกค้าด้วยการเอา uid ไปหาในรายชื่อสมาชิกที่หน้าโหลดมา
ถ้าสมาชิกคนนั้น **ถูกลบไปแล้ว** หรือไม่ได้อยู่ใน 500 คนที่โหลดมา จะโชว์เป็นรหัสดิบยาวๆ
ทั้งที่ตอนบันทึก เซิร์ฟเวอร์เก็บอีเมลของคนนั้นไว้ให้ด้วยแล้ว

เป็นปัญหาเพราะบันทึกมีไว้ใช้ตอนคนหายไปแล้วโดยเฉพาะ (ลบบัญชี / มีเรื่องกัน)
→ ถอยไปใช้อีเมลที่บันทึกไว้ตอนนั้นก่อน ค่อยยอมโชว์รหัสดิบเป็นทางสุดท้าย

### เพิ่ม: ช่องค้นในบันทึก
บันทึก 300 รายการเรียงตามเวลา หาเรื่องของลูกค้าคนเดียวแทบไม่ได้
เพิ่มช่องค้น (ชื่อ / อีเมล / ชื่อการกระทำ) — กรองจากที่โหลดมาแล้ว **ไม่ยิงฐานข้อมูลซ้ำ**
และบอกว่า "แสดง X จาก Y รายการ" กันเข้าใจผิดว่าบันทึกมีอยู่เท่านี้

รวมเทสทั้งเว็บ **1541 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #22 (6 ก.ย. 2569) — แก้แล้ว

### บั๊กจริง: ประวัติของสมาชิกรายคนแสดงยอดต่ำกว่าความจริง
กล่องประวัติกรองเอาจาก 500 รายการล่าสุดของทั้งร้านที่หน้าโหลดมา
ลูกค้าเก่าที่ออเดอร์ไม่ได้อยู่ในก้อนนั้น จะขึ้นว่า **ยอดซื้อสำเร็จ ฿0**
แอดมินเปิดดูแล้วสรุปว่า "ลูกค้าคนนี้ไม่เคยซื้ออะไรเลย" ทั้งที่ซื้อไปเยอะ

→ เปลี่ยนไปถามฐานข้อมูลตรงๆ ด้วย `where(uid)` (ใช้ index เดิมที่หน้าประวัติของลูกค้าใช้อยู่แล้ว
ไม่ต้องสร้างเพิ่ม) ดึงล่าสุด 200 รายการต่อประเภท
ถ้าฐานข้อมูลตอบไม่ได้ ถอยไปใช้ของที่โหลดมา **พร้อมขึ้นคำเตือนว่าตัวเลขอาจต่ำกว่าความจริง**

> หลักที่ใช้ตัดสินทั้ง 3 รอบล่าสุด (17, 21, 22): **ตัวเลขที่ต่ำกว่าความจริงโดยไม่บอก
> อันตรายกว่าไม่โชว์ตัวเลขเลย** เพราะคนอ่านเอาไปตัดสินใจต่อทันที

รวมเทสทั้งเว็บ **1546 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #23 (6 ก.ย. 2569) — แก้แล้ว

### ตัวเลขทั้งหน้าภาพรวมจะผิดเงียบๆ เมื่อร้านโตเกิน 500 รายการ
ยอดขาย · จำนวนออเดอร์ · ยอดเฉลี่ย · จำนวนสมาชิก · **เครดิตคงเหลือรวม** · กราฟทุกใบ
คิดจากข้อมูลที่หน้าดึงมาได้แค่ 500 รายการต่อคอลเลกชัน
ตอนนี้ร้านมีออเดอร์ 11 ใบจึงยังไม่เห็นปัญหา แต่พอโตเกิน 500 ตัวเลขจะเริ่มต่ำกว่าความจริง
**โดยที่หน้าตายังดูปกติทุกอย่าง** — ไม่มีอะไรบอกเลยว่าข้อมูลไม่ครบ

→ เพิ่มคำเตือนบนหน้าภาพรวมเมื่อข้อมูลชนเพดาน บอกด้วยว่าเป็นข้อมูลประเภทไหน
(ออเดอร์ / การเติมเงิน / สมาชิก) และเพดานคือเท่าไหร่

เขียนเป็นกฎเหล็กข้อ 19 ใน CLAUDE.md แล้ว เพราะเป็นรูปแบบความผิดพลาดที่เจอซ้ำ 4 รอบติด

รวมเทสทั้งเว็บ **1552 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #24 (6 ก.ย. 2569) — แก้แล้ว
> เอากฎเหล็กข้อ 19 ที่เพิ่งเขียน มาไล่กับหน้าของ **ลูกค้า** บ้าง

### หน้าประวัติการซื้อ
ดึงมาได้ครั้งละ 200 รายการ ลูกค้าที่ซื้อบ่อยกว่านั้นจะไม่เห็นของเก่า
และ "ยอดซื้อสะสม" ที่โชว์อยู่ก็ต่ำกว่าความจริง **โดยไม่มีอะไรบอกเลย**
→ ขึ้นข้อความว่าแสดง 200 รายการล่าสุด + บอกทางออก (ทักแอดมินพร้อมเลขที่คำสั่งซื้อ)
และต่อท้ายยอดซื้อสะสมว่า "(นับจาก 200 รายการล่าสุด)"

### หน้าเติมเงิน
เดิมดึงมาแค่ **50 รายการ** ด้วยค่าเริ่มต้นแบบเงียบๆ (ไม่ได้ตั้งใจเลือกเลข)
ลูกค้าที่เติมบ่อยจะหารายการเก่าไม่เจอ แล้วเข้าใจว่าเงินที่เคยเติมหายไปจากระบบ
→ ตั้งเป็น 100 อย่างชัดเจน และบอกเมื่อชนเพดาน

### เทสที่เพิ่ม
- `tests/t33-history-cap.mjs` — โหลดหน้าประวัติจริงพร้อมออเดอร์ 200 ใบ แล้วดูว่าบอกลูกค้าจริงไหม
- `tests/t8-wallet.mjs` — เพิ่ม 4 ข้อสำหรับประวัติเติมเงินที่ชนเพดาน

รวมเทสทั้งเว็บ **1562 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #25-26 (6 ก.ย. 2569)

### รอบ 25 — ตรวจหน้าลูกค้าบนเว็บจริง
เปิดหน้าประวัติการซื้อและหน้าเติมเงินบนเว็บจริง: ทำงานปกติ ไม่มี error ในคอนโซล
จอไม่เลื่อนซ้ายขวา ที่สำหรับข้อความ "ประวัติไม่ครบ" ขึ้นครบทั้งสองหน้า
(ตอนตรวจเจอว่าเบราว์เซอร์ยังใช้ไฟล์ที่แคชไว้อยู่ ต้องเปิดลิงก์ใหม่ถึงจะเห็นของล่าสุด — เรื่องเดิมกับรอบ 15)

### รอบ 26 — บันทึกแอดมินก็ชนเพดานเงียบๆ เหมือนกัน
แท็บบันทึกดึงมาได้ 300 รายการ และ **ช่องค้นกรองได้เฉพาะในก้อนที่ดึงมาแล้ว**
ถ้าไม่บอก แอดมินจะค้นเรื่องเก่าไม่เจอแล้วสรุปว่า "ไม่มีบันทึก" ทั้งที่มีอยู่
→ ขึ้นคำเตือนเมื่อชนเพดาน พร้อมบอกว่าการค้นครอบคลุมแค่ก้อนนี้

### ตรวจแล้วไม่พบปัญหา
`fetchProducts` กับ `fetchStockItems` ไม่ได้ตั้งเพดานไว้ (ดึงมาทั้งหมด) จึงไม่มีปัญหาตัวเลขไม่ครบ

รวมเทสทั้งเว็บ **1567 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #27 (6 ก.ย. 2569) — แก้แล้ว

### บั๊กจริง: เครดิตคืนเข้ากระเป๋าโดยไม่มีอะไรอธิบายที่มา
ถ้าแอดมินซ่อนออเดอร์ไว้ แล้วภายหลังกด "ยกเลิก + คืนเครดิต"
ลูกค้าจะเห็นเครดิตเพิ่มขึ้นเฉยๆ แต่เปิดประวัติแล้วไม่เจอออเดอร์ใบไหนที่อธิบายได้เลย
(การยกเลิกไม่ได้สร้างรายการในประวัติเติมเงินด้วย — ที่อธิบายได้มีแค่ตัวออเดอร์เอง)
กลายเป็นเรื่องกันทีหลังว่าเงินมาจากไหน / ทำไมของที่สั่งหายไป

→ **ยกเลิก + คืนเครดิต จะเลิกซ่อนให้เองเสมอ** ทำในคำสั่งเดียวกับที่คืนเงิน (transaction เดียว)

deploy เซิร์ฟเวอร์แล้ว · รวมเทสทั้งเว็บ **1570 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #28 (6 ก.ย. 2569) — แก้แล้ว

### ต่อจากรอบ 27: ไม่ใช่แค่การยกเลิกที่ทำให้เครดิตเปลี่ยนโดยไม่มีที่มา
ไล่ดูทุกรายการที่ซ่อนได้ แล้วถามว่า "ซ่อนไปแล้วลูกค้ายังอธิบายยอดเครดิตของตัวเองได้ไหม"
- **ออเดอร์ที่สำเร็จ** = เครดิตถูกหักไปแล้ว ซ่อนแล้วลูกค้าเห็นยอดน้อยลงโดยหาที่มาไม่เจอ
- **เติมเงินที่อนุมัติแล้ว** (รวมที่แอดมินปรับเครดิตให้เอง) = เครดิตเข้าไปแล้ว เหมือนกัน
- ออเดอร์ที่ยกเลิก / เติมเงินที่ไม่อนุมัติ = ไม่ได้ขยับเครดิต ซ่อนได้ไม่มีปัญหา
  (และนี่คือกลุ่มที่แอดมินอยากซ่อนจริงๆ อยู่แล้ว — รายการที่ยิงซ้ำ/สั่งผิด)

→ ก่อนซ่อนรายการที่ขยับเครดิตจริง ขึ้นคำเตือนเฉพาะว่า
**"รายการนี้คือหลักฐานชิ้นเดียวที่อธิบายว่าเครดิตเปลี่ยนไปเพราะอะไร"**

เลือก "เตือนให้ชัด" ไม่ใช่ "ห้ามซ่อน" — เพราะเป็นร้านของผู้ใช้ ผู้ใช้ควรตัดสินใจเอง
แต่ต้องรู้ผลที่ตามมาก่อนกด (ถ้าอยากให้ห้ามไปเลย บอกได้)

รวมเทสทั้งเว็บ **1571 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #29 (6 ก.ย. 2569) — แก้แล้ว
> รอบนี้: อ่านสิ่งที่ตัวเองแก้ทั้งหมดในวันนี้ใหม่ตั้งแต่ต้น เหมือนเป็นคนอื่นมารีวิว

### บั๊กจริงที่ตัวเองเพิ่งสร้างขึ้นมาเอง — หน้าหลังบ้านว่างเปล่าช่วง 10 นาทีหลัง deploy
ไฟล์ `.html` กับ `.js` ถูกแคชแยกกัน GitHub Pages เก็บไว้ราว 10 นาที
คนที่เปิดเว็บค้างไว้แล้วโหลดใหม่ในช่วงนั้น จะได้ **หน้าเก่า + สคริปต์ใหม่** มาเจอกัน

สคริปต์ใหม่ผูกปุ่มกับ element ที่หน้าเก่ายังไม่มี → พังตั้งแต่บรรทัดนั้น
**ผลไม่ใช่แค่ปุ่มใหม่ยังไม่มา แต่คือหน้าหลังบ้านว่างเปล่าทั้งหน้า** เพราะโมดูลหยุดกลางคัน
ทดสอบแล้วเห็นชัด: ถอดโค้ดกันออก → แดชบอร์ดไม่ขึ้น ตารางไม่มีสักตาราง

เจอเรื่องเดียวกันในหน้าประวัติของลูกค้าด้วย (`history-note`)

→ element ที่เพิ่งเพิ่มเข้ามาทุกตัว ใช้ `?.` หรือเช็ค null ก่อน
→ กล่องข้อความ (`setMsg`) ข้ามไปเงียบๆ ถ้าไม่มีที่ให้แสดง
→ กล่องประวัติสมาชิกที่เปิดไม่ได้ บอกให้กด Ctrl+F5 แทนที่จะเงียบ
→ เขียนเป็นกฎเหล็กข้อ 20 ใน CLAUDE.md

### เทสที่เพิ่ม
`tests/t34-stale-html.mjs` — ลบของใหม่ออกจาก admin.html แล้วโหลดสคริปต์ใหม่ทับ
ตรวจว่าแดชบอร์ด ตารางทั้งสาม การ์ดตัวเลข ปุ่ม และการสลับแท็บ ยังทำงานครบ
(พิสูจน์แล้วว่าจับได้จริง — ถอดโค้ดกันออก เทสล้ม 5 ข้อทันที)

รวมเทสทั้งเว็บ **1584 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #30 (6 ก.ย. 2569) — แก้แล้ว
> รอบนี้: เอาบทเรียนจากรอบ 29 มาไล่ทั้งเว็บ ไม่ใช่แก้เฉพาะที่เจอ

### เพิ่มตัวไล่แบบกลไก
ให้เครื่องไล่ทุกไฟล์เอง: หา `getElementById("ของใหม่").` ที่ไม่มีการเช็คก่อนใช้
**เจอเพิ่มอีก 1 จุดที่ตาคนมองข้ามไป** — บรรทัดนับผลการค้นหาในฟังก์ชันค้นทั้งฐานข้อมูล
(ทั้งตอนค้นสำเร็จและตอนค้นไม่สำเร็จ) แก้แล้วทั้งสองที่

### ขยายเทสหน้าเก่าเจอสคริปต์ใหม่ ให้ครอบหน้าลูกค้าด้วย
หน้าหลังบ้านพังกระทบแค่เจ้าของร้าน แต่หน้าประวัติ/เติมเงินพังกระทบลูกค้าทุกคน

รวมเทสทั้งเว็บ **1588 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #31-32 (6 ก.ย. 2569)

### รอบ 31 — ตรวจสไตล์ของใหม่ ไม่พบปัญหา
- คำเตือนทุกอันใช้คลาส `.msg.warn` ที่มีสไตล์รองรับอยู่แล้ว
- CSS ที่เพิ่มใหม่ทั้งหมดใช้ตัวแปรสี ไม่มีสีตายตัวสักจุด → โหมดมืดถูกต้องเอง

### รอบ 32 — ลองใช้จริงบนเว็บ production ด้วยบัญชีแอดมิน (อ่านอย่างเดียว ไม่กดปุ่มที่แก้ข้อมูล)
**ของใหม่ทำงานครบทุกอย่างบนของจริง:**
- แท็บบันทึกแอดมิน: โหลดบันทึกจริง 13 รายการ แถวแรกอ่านได้ว่า
  "ทำเสร็จแล้ว · 908wayu@gmail.com · ลูกค้า: วายุ พลศิริ · เลขที่ 7665B938"
- กล่องประวัติสมาชิก: ดึงจากฐานข้อมูลสำเร็จ (ไม่ขึ้นคำเตือน)
  ตัวเลขสมเหตุสมผลกันเอง — เติมมา ฿940 ซื้อไป ฿940 เหลือ ฿0
- ค้นด้วยเลขที่จริง: เจอ 1 รายการ
- ค้นเลขที่ที่ไม่มี: ขึ้น "ไม่พบใน 11 รายการล่าสุดที่โหลดมา" + ปุ่มค้นทั้งฐานข้อมูล
  กดแล้วขึ้น "ค้นทั้งฐานข้อมูลแล้วไม่มีออเดอร์เลขที่นี้จริงๆ"
  → ข้อความ 3 แบบที่ตั้งใจแยกไว้ ทำงานถูกต้องครบบนของจริง

---
## รอบรีวิวโค้ดตัวเอง #33 (6 ก.ย. 2569) — แก้แล้ว
> รอบนี้ย้ายมาไล่หน้าร้าน (`app.js`) — จุดที่ลูกค้าเสียเงินจริง

### บั๊กจริง: ของหมดระหว่างที่กล่องสั่งซื้อเปิดค้างอยู่ แล้วจำนวนกลายเป็น 0
ลำดับการบีบค่าจำนวนเขียนกลับด้าน (`min` ไว้ท้ายแทนที่จะเป็น `max`)
พอสินค้าหมดระหว่างที่ลูกค้าเปิดกล่องค้างไว้ (คนอื่นซื้อตัดหน้าไปพอดี) เพดานจำนวนกลายเป็น 0
ลูกค้ากดปุ่มเพิ่ม/ลดจำนวนต่ออีกที จำนวนจะถูกบีบเหลือ **0** แล้วเกิดต่อเนื่องกันสามอย่าง:
1. ยอดรวมกลายเป็น **฿0**
2. คำเตือน "ของไม่พอ" หายไป (เพราะเงื่อนไขเช็ค `จำนวน > สต๊อก` และ 0 ไม่มากกว่า 0)
3. ข้อความเปลี่ยนเป็น "ต้องติ๊กยอมรับเงื่อนไขก่อน" = **ชวนให้ลูกค้ากดสั่งซื้อของที่หมดแล้วในราคา ฿0**

(เซิร์ฟเวอร์ปฏิเสธอยู่แล้วจึงไม่เสียเงินจริง แต่ลูกค้าเจอหน้าจอที่ดูเหมือนเว็บพัง)

เกิดได้จริงเพราะหน้าร้านดึงสินค้ามาใหม่ทันทีหลังกดยืนยันไม่ผ่าน — ทั้งที่กล่องยังเปิดอยู่

→ แก้ให้บีบ "ไม่ต่ำกว่า 1" เป็นขั้นสุดท้ายเสมอ ของหมดจึงกลับไปเตือนว่าของไม่พอและปุ่มกดไม่ได้
พิสูจน์แล้วว่าเทสจับได้ — ถอดโค้ดที่แก้ออก เทสล้ม 4 ข้อทันที

รวมเทสทั้งเว็บ **1594 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #34 (6 ก.ย. 2569) — ตรวจแล้วไม่พบปัญหา

สงสัยว่ากล่องสั่งซื้อจะไม่อัปเดตเมื่อเครดิตเข้าระหว่างที่เปิดค้างอยู่
(เพิ่งเติมเงินอีกแท็บ / แอดมินเพิ่งปรับให้ / โปรไฟล์เพิ่งโหลดเสร็จ)
เพราะเห็นว่า `authchange` ผูกไว้กับ `syncNav` อย่างเดียว

**แก้ไปแล้วถึงรู้ว่าไม่ใช่บั๊ก** — `syncNav` ปิดท้ายด้วย `if (BUY) syncBuy();` อยู่แล้ว
ทดสอบยืนยัน: ถอดโค้ดที่เพิ่งเพิ่มออก เทสยังผ่าน = ของเดิมทำงานถูกอยู่แล้ว
→ **ถอนการแก้ออก** (เพิ่มไปก็แค่เรียกซ้ำสองรอบเปล่าๆ)

**เก็บเทสไว้** 3 ข้อ เพราะพฤติกรรมนี้สำคัญและไม่เคยมีเทสคุ้มกันมาก่อน:
เครดิตเข้าระหว่างเปิดกล่อง → ตัวเลขอัปเดตเอง · ปุ่มกลับมากดได้เอง · ข้อความ "เครดิตไม่พอ" หายไป

> บันทึกไว้เตือนตัวเอง: อ่านฟังก์ชันที่เกี่ยวข้องให้จบก่อนสรุปว่าเป็นบั๊ก

---
## รอบรีวิวโค้ดตัวเอง #35 (6 ก.ย. 2569) — แก้แล้ว

### บั๊กเดิมของรอบ 33 ยังเหลืออยู่อีกที่ — ที่ตัวเองแก้ไม่ครบเอง
การบีบค่าจำนวนเขียนไว้ **สองที่** คือปุ่มเพิ่ม/ลด กับช่องพิมพ์จำนวนเอง
รอบ 33 แก้แค่ปุ่ม ส่วนช่องพิมพ์ยังผิดลำดับเหมือนเดิม
→ ลูกค้าที่พิมพ์จำนวนเองแทนการกดปุ่ม ยังเจอ ฿0 กับปุ่มยืนยันที่กลับมากดได้อยู่ดี

→ ยุบให้เหลือฟังก์ชันเดียว (`clampQty`) ทั้งสองที่เรียกตัวเดียวกัน
จะได้ไม่มีทางแก้ที่หนึ่งแล้วลืมอีกที่อีก

เพิ่มเทสสำหรับช่องพิมพ์จำนวนแยกจากปุ่ม (เป็นโค้ดคนละก้อน ต้องตรวจคนละที)
พิสูจน์แล้ว: กลับลำดับเดิม → เทสล้ม 6 ข้อ (ครอบทั้งปุ่มและช่องพิมพ์)

รวมเทสทั้งเว็บ **1600 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #36 (6 ก.ย. 2569) — เครื่องมือกันพลาด
> เอาบทเรียนรอบ 35 (แก้ที่หนึ่งแล้วลืมอีกที่) มาทำเป็นตัวไล่ให้เครื่องตรวจแทน

เพิ่ม `tests/t35-shared-rules.mjs` เทียบ "กฎเดียวกันที่เขียนไว้หลายไฟล์" ว่ายังตรงกันอยู่:
- **สูตรเลขที่คำสั่งซื้อ** (app.js / purchases.js / admin.js)
  *ถ้าไม่ตรงกัน ลูกค้าแจ้งเลขมาแล้วแอดมินหาไม่เจอ = เถียงกันว่าไม่เคยสั่ง*
- **ฟังก์ชันหนีอักขระ** ทั้ง 4 ไฟล์ ต้องครอบตัวอักษรชุดเดียวกันครบ 5 ตัว
  *ไฟล์ไหนตกไปตัวหนึ่ง ไฟล์นั้นกลายเป็นช่องยัดสคริปต์ทันที (กฎเหล็กข้อ 3)*
- **กลุ่มสถานะออเดอร์** (admin / purchases / auth / worker)
  *ถ้าที่ไหนตก `approved` ออก ประวัติการขายของออเดอร์เก่าจะหายทั้งก้อน*
- **เวลาเคลม** ต้องอ่านจาก shop-config ไม่ใช่เขียนตัวเลขตายในโค้ด

พิสูจน์แล้ว: แกล้งตัดเครื่องหมาย ' ออกจากฟังก์ชันหนีอักขระของ admin.js
และแกล้งตัด `approved` ออกจาก purchases.js → เทสจับได้ทั้งสองอย่างทันที

เขียนเป็นกฎเหล็กข้อ 21 ใน CLAUDE.md

รวมเทสทั้งเว็บ **1619 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #37-38 (6 ก.ย. 2569)

### รอบ 37 — ไล่เส้นทางเติมเงินทั้งเส้น ไม่พบปัญหา
- ปุ่มถูกปิดก่อนยิงทุกเส้นทาง (ทั้งแบบแนบสลิปและแบบยิงซองให้บอทกดรับ) กดซ้ำไม่ได้
- รูปสลิปถูกย่อและปฏิเสธตั้งแต่ฝั่งเบราว์เซอร์ถ้าใหญ่เกิน (700KB) ซึ่งต่ำกว่าเพดานของกฎ (1MB)
  ลูกค้าจึงเห็นข้อความ "รูปใหญ่เกินไป" ไม่ใช่ข้อความสิทธิ์ที่อ่านไม่รู้เรื่อง
- สลิปถูกเขียนแยกเอกสารจริง ไม่ได้ฝังในรายการเติมเงิน (ตามกฎเหล็กข้อ 9)

### รอบ 38 — ไล่หาจุดที่ต่อ HTML โดยไม่หนีอักขระ
เขียนตัวสแกนไล่ทุกไฟล์ฝั่งลูกค้า หา `${...}` ที่ไม่ได้ผ่านตัวหนีอักขระ
ตัดตัวเลข/ข้อความคงที่/ตัวช่วยที่ปลอดภัยออก เหลือให้ดูด้วยตา 51 จุด

**ทั้งหมดปลอดภัยอยู่แล้ว ยกเว้นป้ายของกราฟเส้นในหน้าภาพรวม** ที่ใส่ตรงๆ ไม่ผ่านตัวหนีอักขระ
(ทั้งบนแกนกราฟและในกล่องที่โผล่ตอนเอาเมาส์ชี้)

วันนี้ยังไม่เป็นช่องโหว่จริง เพราะป้ายของกราฟเส้นเป็นวันที่ล้วน
แต่กราฟแท่งที่อยู่ข้างๆ กันใช้ **ชื่อสินค้า** เป็นป้ายและหนีอักขระไว้แล้ว
ถ้าวันหน้ามีใครเปลี่ยนที่มาของป้ายกราฟเส้น จะกลายเป็นช่องยัดสคริปต์แบบไม่มีใครทันสังเกต
→ หนีอักขระไว้ทั้งสองที่ + เพิ่มเทสว่าต้องไม่มีป้ายกราฟไหนใส่ตรงๆ อีก

รวมเทสทั้งเว็บ **1622 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #39 (6 ก.ย. 2569) — แก้แล้ว (เจอ 2 เรื่อง)
> รอบนี้ไล่เส้นทางกดรับซองอั่งเปาที่เซิร์ฟเวอร์ — อีกทางที่เงินเข้าร้านจริง

### บั๊กจริงที่ 1: สมาชิกผีที่มีแต่ยอดเครดิต
เส้นทางนี้เช็คว่ามีเอกสารสมาชิกอยู่จริง **ตอนต้นทาง** แล้วค่อยไปกดรับซอง
ซึ่งใช้เวลาหลายวินาที (ต้องรอทรูมันนี่ตอบ) — เอกสารสมาชิกถูกลบระหว่างนั้นได้
คำสั่งบวกเครดิตของ Firestore **สร้างเอกสารให้ถ้าไม่มี** = ได้สมาชิกที่มีแต่ยอดเครดิต
ไม่มีชื่อ ไม่มีอีเมล ไม่มีสิทธิ์ โผล่ในหลังบ้านแบบว่างเปล่า (เรื่องเดียวกับรอบ 13 คนละที่)

→ ใส่เงื่อนไข "ต้องมีเอกสารอยู่จริง" ที่คำสั่งบวกเครดิตด้วย
ถ้าไม่ผ่าน คำสั่งทั้งชุดถูกปฏิเสธแล้วตกไปทางสำรองที่มีอยู่แล้ว:
**พักรายการไว้เป็น "รออนุมัติ" พร้อมยอดจริง** ให้แอดมินกดเอง (เงินเข้าร้านไปแล้ว ห้ามทิ้ง)

### บั๊กจริงที่ 2 — อยู่ในตัวเทสเอง: commit ของตัวจำลองไม่ atomic
Firestore commit เป็น all-or-nothing แต่ตัวจำลองไล่เขียนทีละคำสั่ง
แล้วค่อยเจอเงื่อนไขที่ไม่ผ่านกลางทาง = คำสั่งก่อนหน้าถูกเขียนไปแล้ว
ทำให้เทสเห็นสถานะที่**เป็นไปไม่ได้จริง** (รายการขึ้นว่า "อนุมัติแล้ว" ทั้งที่ commit ล้มเหลว)
แล้วสรุปผลผิด

เจอเพราะเทสใหม่ล้มด้วยเหตุผลที่ไม่สมเหตุสมผล จึงตามไปดูตัวจำลองแทนที่จะแก้เทสให้ผ่านๆ ไป
→ แก้ทั้ง `t3-worker` และ `t4-angpao` ให้ตรวจเงื่อนไขของทุกคำสั่งให้ครบก่อน แล้วค่อยเขียน

พิสูจน์แล้ว: ถอดเงื่อนไขที่แก้ออก → ได้สมาชิกผี `{credit: 77}` ขึ้นมาจริงๆ และระบบตอบว่าสำเร็จ

deploy เซิร์ฟเวอร์แล้ว · รวมเทสทั้งเว็บ **1627 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #40-41 (6 ก.ย. 2569) — ตรวจแล้วไม่พบปัญหา

### รอบ 40 — ไล่ดูว่าตัวจำลอง Firestore ยัง "ใจดีเกินจริง" ตรงไหนอีกไหม
(ต่อจากรอบ 39 ที่เจอว่า commit ไม่ atomic)
ตรวจแล้วตรงกับของจริงครบ: แก้เอกสารที่ไม่มีอยู่แล้วต้องพัง · เขียนทับทั้งเอกสารเมื่อไม่ใส่ merge ·
ปฏิเสธค่า undefined · ตัดเอกสารที่ไม่มีฟิลด์ที่ใช้เรียง · ตรวจกฎกับ "คำสั่ง" ไม่ใช่ผลลัพธ์
และโค้ดจริงไม่ได้ใช้ท่าที่ตัวจำลองยังไม่รองรับ (เขียนฟิลด์ซ้อนด้วยจุด / arrayUnion) เลยสักที่

### รอบ 41 — ลองกล่องสั่งซื้อบนเว็บจริง (ไม่กดยืนยัน)
ยืนยันว่าที่แก้ในรอบ 33/35 ทำงานถูกบนของจริง:
- กดเพิ่ม 3 ครั้ง → 4 · กดลด 9 ครั้ง → 1 (ไม่ต่ำกว่า 1 สักครั้ง)
- พิมพ์ `0` / ติดลบ / ตัวอักษร → ยอดรวมยังเป็นราคา 1 ชิ้น ไม่กลายเป็น ฿0
- พิมพ์ 99999 → ยอดถูกจำกัดที่สต๊อกจริง (50 ชิ้น = ฿2,475)

---
## รอบรีวิวโค้ดตัวเอง #42 (6 ก.ย. 2569) — เครื่องมือกันพลาด (หน้าเงื่อนไข)

หน้าเงื่อนไข/นโยบายเป็นข้อผูกพันกับลูกค้าจริง เดิมเทสตรวจแค่ว่า
"มีหัวข้อครบ" และ "จำนวนการ์ดเท่ากันทั้งสองภาษา" ซึ่งยังหลุดได้
ถ้าฝั่งไทยมีข้อย่อยหนึ่งข้อแต่ฝั่งอังกฤษไม่มี → ลูกค้าคนละภาษาเข้าใจเงื่อนไขไม่ตรงกัน
แล้วกลายเป็นเรื่องกันตอนขอคืนเงิน

เพิ่มการตรวจ:
- **ข้อย่อยในแต่ละหัวข้อต้องเท่ากันทั้งสองภาษา** (ไม่ใช่แค่จำนวนหัวข้อ)
- **ตัวเลขในเงื่อนไข** (นาที/วัน/บาท) ต้องตรงกันทั้งสองภาษา
- **ปีที่แก้ไขล่าสุดต้องเป็นคู่ พ.ศ./ค.ศ. ที่ตรงกันจริง** (ต่างกัน 543 พอดี)
  กันกรณีอัปเดตฝั่งเดียวแล้วอีกฝั่งค้างปีเก่า

พิสูจน์แล้ว: แกล้งลบข้อย่อยหนึ่งข้อออกจากฝั่งอังกฤษ
→ เทสฟ้องทันทีว่า "สั่งซื้อและจ่ายเงินอย่างไร (ไทย 6 / อังกฤษ 5)"

รวมเทสทั้งเว็บ **1630 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #43 (6 ก.ย. 2569) — ปิดช่องว่างที่ไม่มีเทสคุมเลย

หน้า `privacy.html` เป็นเอกสารที่ผูกพันกับลูกค้าเหมือน `terms.html`
แต่ **ไม่มีเทสคุมสักข้อ** ทั้งที่ใช้โครงสองภาษาแบบเดียวกันเป๊ะ

เพิ่ม `tests/t36-privacy.mjs` (21 ข้อ) ใช้เกณฑ์เดียวกับหน้าเงื่อนไข:
- โครงสองภาษาถูกต้อง ไม่โชว์ซ้อนกัน สลับภาษาได้
- มีเนื้อหาที่นโยบายความเป็นส่วนตัวต้องมี ครบทั้งสองภาษา
- **ข้อย่อยในแต่ละหัวข้อเท่ากันทั้งสองภาษา**
  *ถ้าฝั่งไทยบอกว่าเก็บข้อมูล 6 อย่างแต่อังกฤษบอก 5 = ลูกค้าที่อ่านอังกฤษไม่รู้ว่าร้านเก็บอะไรอีก*
- ปีที่ปรับปรุงล่าสุดเป็นคู่ พ.ศ./ค.ศ. ที่ตรงกัน (กันอัปเดตฝั่งเดียว)

พิสูจน์แล้ว: แกล้งลบข้อย่อยจากฝั่งอังกฤษ → เทสฟ้อง "ข้อมูลที่เราเก็บ (ไทย 5 / อังกฤษ 4)"

รวมเทสทั้งเว็บ **1651 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #44 (6 ก.ย. 2569) — ปิดช่องว่างใน QR พร้อมเพย์

### ช่องว่างที่เจอ: ไม่มีใครตรวจว่า "เลขตรวจสอบ" ท้าย QR ถูกจริงหรือเปล่า
เดิมมีเทสพร้อมเพย์ 20 กว่าข้อ แต่ทุกข้อตรวจแค่ **โครง** ของ payload:
ขึ้นต้นถูกไหม มีช่องสกุลเงินไหม ยอดเงินทศนิยม 2 ตำแหน่งไหม ลงท้ายด้วย 4 หลักไหม

ถ้าสูตรเลขตรวจสอบ (CRC) ผิดไปตัวเดียว **เทสจะยังผ่านหมดทุกข้อ**
แต่แอปธนาคารของลูกค้าจะปฏิเสธ QR ทุกใบ แล้วไม่มีใครรู้ว่าทำไม
(ลูกค้าสแกนไม่ผ่าน = เติมเงินไม่ได้ = ร้านไม่ได้ขาย)

→ เขียนสูตรขึ้นมาใหม่ในเทส **ด้วยวิธีคนละแบบ** (ใช้ตารางค่าล่วงหน้า แทนการเลื่อนบิตทีละตัว)
แล้วเทียบกัน — ถ้าลอกสูตรเดียวกันมาวาง จะผิดเหมือนกันทั้งคู่โดยไม่มีใครจับได้
พร้อมตรวจสูตรในเทสเองด้วยค่ามาตรฐานสากล (`123456789` → `29B1`)

ตรวจครบ 5 แบบ: ไม่ระบุยอด · ระบุยอดมีทศนิยม · ยอดเต็มจำนวน · ชื่อร้านยาว · เลขบัตรประชาชน

พิสูจน์แล้ว: แกล้งแก้สูตรผิดไปตัวเดียว (เลื่อน 7 บิตแทน 8) → เทสใหม่จับได้ทันที
ขณะที่เทสโครงสร้าง 20 กว่าข้อเดิม **ผ่านหมดเหมือนไม่มีอะไรเกิดขึ้น**

รวมเทสทั้งเว็บ **1658 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #45 (6 ก.ย. 2569) — กฎเหล็กข้อ 8 ไม่มีเทสคุมเลย

กฎเหล็กข้อ 8 เขียนไว้ว่า *"วันที่ต้องใช้เวลาท้องถิ่น ไม่ใช่ UTC — เคยทำให้ออเดอร์ตอนเช้าหายไป"*
คือบั๊กที่**เคยเกิดขึ้นจริงมาแล้ว** แต่ไล่ดูแล้ว **ไม่มีเทสสักข้อที่กันไม่ให้มันกลับมา**

ไทยเป็น UTC+7 ถ้าแบ่งวันด้วยเวลา UTC ออเดอร์ที่สั่งก่อน 07:00 น. ตามเวลาไทย
จะถูกนับเป็น "เมื่อวาน" → ยอดขายรายวันเลื่อนไปทั้งแถบโดยไม่มีใครทันสังเกต

เพิ่มเทส 6 ข้อ:
- 00:30 / 06:00 / 23:30 ตกอยู่ในวันที่ถูกต้อง
- ผ่านของจริง: ออเดอร์ตอนตี 1 ของวันนี้ ต้องอยู่ในถังของวันนี้ และไม่มีถังอื่นได้ค่าเกินมา
- **ข้อที่พิสูจน์ว่าเทสมีความหมาย**: เทียบกับวิธีที่ผิด (ตัดวันจากเวลา UTC)
  แล้วยืนยันว่าให้ผลคนละวันจริงในโซนเวลาไทย
  (ถ้าเครื่องที่รันเทสตั้งเป็น UTC พอดี ข้อนี้จะข้ามไปพร้อมบอกเหตุผล ไม่ใช่ผ่านแบบหลอกๆ)

พิสูจน์แล้ว: แกล้งเปลี่ยนกลับไปใช้ UTC → ออเดอร์ตอนตี 1 ของวันที่ 6 ตกไปอยู่ถังวันที่ 5
ซึ่งคือบั๊กเดิมเป๊ะๆ ที่เคยเกิดขึ้น

รวมเทสทั้งเว็บ **1664 ข้อ ผ่านหมด**

---
## รอบรีวิวโค้ดตัวเอง #46 (6 ก.ย. 2569) — ไล่กฎเหล็กทั้ง 21 ข้อว่ามีเทสคุมครบไหม

ต่อจากรอบ 45 ที่เจอว่ากฎเหล็กข้อ 8 ไม่มีเทสคุมเลย จึงไล่ให้ครบทุกข้อ
ส่วนใหญ่มีเทสคุมอยู่แล้ว **แต่เจอ 2 ข้อที่ไม่มี**:

### กฎข้อ 17 (กดข้ามขั้นไม่ได้) — ฝั่งเซิร์ฟเวอร์
เซิร์ฟเวอร์กันไว้ถูกต้องอยู่แล้ว แต่ไม่มีเทสข้อไหนคุมเลย
เพิ่ม 10 ข้อ: ลัดจากรอดำเนินการไปสำเร็จไม่ได้ · กดซ้ำไม่ได้ · ย้อนกลับไม่ได้ ·
ออเดอร์ที่ยกเลิกแล้วเดินต่อไม่ได้ · ปุ่มชุดใหม่ใช้กับออเดอร์เก่าไม่ได้

### กฎข้อ 16 (ห้ามสลับปุ่มสองระบบ) — ฝั่งหน้าเว็บ
เอกสารเขียนไว้ชัดว่า *"เซิร์ฟเวอร์กันไว้อีกชั้น แต่หน้าเว็บต้องไม่วาดปุ่มผิดชุดตั้งแต่แรก"*
แต่ไม่มีเทสตรวจว่าหน้าเว็บวาดปุ่มถูกจริงไหม
เพิ่ม 10 ข้อ ตรวจชุดปุ่มของทุกสถานะแบบเป๊ะๆ (ไม่ใช่แค่ "มีปุ่มอยู่"):
รอดำเนินการ / กำลังดำเนินการ / สำเร็จ / ยกเลิกแล้ว ทั้งออเดอร์ใหม่และเก่า
รวมถึงข้อที่อันตรายที่สุด — **ห้ามมีปุ่ม "อนุมัติ" โผล่ในออเดอร์ใหม่ เพราะจะหักเครดิตซ้ำสองรอบ**

พิสูจน์แล้ว: แกล้งสลับให้ออเดอร์ใหม่โชว์ปุ่ม "อนุมัติ" → เทสจับได้ทันที 2 ข้อ

รวมเทสทั้งเว็บ **1684 ข้อ ผ่านหมด**
