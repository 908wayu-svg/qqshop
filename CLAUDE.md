# CLAUDE.md

## ขอบเขตการทำงาน
- ทำงานเฉพาะในโฟลเดอร์นี้ (OMG HHH) เท่านั้น ห้ามแก้ไข/สร้าง/ลบไฟล์นอกโฟลเดอร์นี้
- ผู้ใช้ไม่มีความรู้ด้านโค้ด/เครื่องมือ — Claude ต้องลงมือทำทุกขั้นตอนเอง (เขียนโค้ด, รันคำสั่ง, ติดตั้ง
  แพ็กเกจ, ทดสอบ, deploy) โดยไม่ขอให้ผู้ใช้พิมพ์คำสั่งเอง ยกเว้นขั้นตอนที่ทำแทนไม่ได้จริงๆ (เช่น ล็อกอิน/
  ยืนยันตัวตนผ่านเบราว์เซอร์)

## สไตล์การสื่อสาร
- ภาษาไทย กระชับ ตรงประเด็น "เอาเนื้อไม่เอาน้ำ" ห้ามอธิบายยาว ห้ามพูดซ้ำสิ่งที่ทำไปแล้ว
- หลังทำงานเสร็จ สรุปผลสั้นๆ พอ (ทำอะไร / ลิงก์ผลลัพธ์ถ้ามี) ไม่ต้องเกริ่นนำก่อนเริ่มงาน
- ถามคำถามเฉพาะตอนตัดสินใจแทนไม่ได้จริงๆ เท่านั้น ไม่ถามพร่ำเพรื่อ

---

# QQSHOP — ร้านขายไอดีเกม/สินค้าดิจิทัล

เว็บ: https://908wayu-svg.github.io/qqshop/ · repo: `908wayu-svg/qqshop`

## ภาพรวมระบบ
หน้าเว็บเป็น HTML/CSS/JS ธรรมดาบน GitHub Pages (ไม่มี build step ไม่มี framework)
แต่ **ไม่ใช่เว็บ static ล้วน** — มีของจริงอยู่ 2 ฝั่ง:

| ส่วน | ที่อยู่ | ทำอะไร |
|---|---|---|
| หน้าเว็บ | GitHub Pages | แสดงผล + เรียก Firebase/Worker |
| ฐานข้อมูล + ระบบสมาชิก | Firebase project `qqshop-ecc92` | Firestore + Authentication |
| เซิร์ฟเวอร์ | Cloudflare Worker (โฟลเดอร์ `worker/`) | สร้างออเดอร์ + รับซองอั่งเปา + **งานแอดมินทุกอย่างที่แตะเงิน/สิทธิ์** |

**ไม่ใช้เงินสด/QR จ่ายตรงแล้ว** ลูกค้าเติมเครดิตเข้ากระเป๋าก่อน แล้วใช้เครดิตซื้อของ

## ไฟล์สำคัญ
- `index.html` + `app.js` — หน้าร้าน + **หน้าต่างสั่งซื้อทันที** (ไม่มีตะกร้าแล้ว)
  กด "สั่งซื้อ" ที่การ์ดสินค้า → เปิดกล่องของชิ้นนั้น → เลือกจำนวน + กรอกไอดีเกม (ถ้าสินค้าขอ)
  + **ติ๊กยอมรับเงื่อนไขการเคลม** → กดยืนยัน → เครดิตถูกหักทันที
- `wallet.html` + `wallet.js` — เติมเงิน (ทรูวอลเล็ต / ธนาคาร / พร้อมเพย์ / ซองอั่งเปา)
- `purchases.html` + `purchases.js` — ประวัติการซื้อ + กดดูไอดี/รหัสผ่านที่ซื้อ
  + นับถอยหลังเวลาแจ้งเคลม + **ลูกค้าแก้ไอดีเกมของตัวเองได้ตอนสถานะ "รอดำเนินการ"**
- `admin.html` + `admin.js` — หลังบ้าน 6 แท็บ (ภาพรวม/ออเดอร์/เติมเงิน/สินค้า/สมาชิก/บันทึกแอดมิน)
  + **ช่องค้นหาออเดอร์** — ใส่เลขที่คำสั่งซื้อที่ลูกค้าแจ้งมาได้เลย (หรือชื่อ/อีเมล/ชื่อสินค้า)
    ตอนค้นหาจะมองข้ามตัวกรองสถานะ เพื่อให้เจอออเดอร์ที่ยกเลิกไปแล้วด้วย
    ถ้าไม่เจอในหน้า มีปุ่ม **"ค้นทั้งฐานข้อมูล"** ให้กด (หน้านี้โหลดมาแค่ 500 ใบล่าสุด)
    ค้นด้วยช่วงของรหัสเอกสาร — ใช้ได้เพราะรหัสออเดอร์ที่เซิร์ฟเวอร์สร้างเป็นเลขฐานสิบหกตัวเล็กล้วน
    เลขที่ที่ลูกค้าเห็นจึงเป็น "ส่วนหน้าของรหัส" พอดี (ไม่ต้องเพิ่มฟิลด์ ไม่ต้องสร้าง index)
  + **ปุ่ม "ประวัติ" ในแท็บสมาชิก** — เปิดกล่องดูออเดอร์+การเติมเงินของคนนั้นคนเดียว
    ใช้ข้อมูลที่โหลดมาแล้วในหน้า ไม่ยิงฐานข้อมูลเพิ่ม (จึงเห็นเท่าที่อยู่ใน 500 รายการล่าสุดของร้าน)
  + **แท็บบันทึกแอดมิน** — อ่าน `adminLogs` โหลดตอนเปิดแท็บครั้งแรกเท่านั้น
  + **ปุ่มซ่อน/เลิกซ่อน** ในแถวออเดอร์และเติมเงิน (ดูฟิลด์ `hiddenAt` ด้านล่าง)
  + กดดูสลิปแล้วมีตัวอ่านตัวเลขในรูปให้อัตโนมัติ (ยอด/วันที่/เวลา/ชื่อผู้โอน)
  ทำงานในเบราว์เซอร์ล้วนๆ ด้วย Tesseract.js — **เป็นแค่ตัวช่วยอ่าน ไม่ใช่การยืนยันว่าสลิปจริง**
  แอดมินยังต้องเทียบกับรายการโอนจริงในแอปธนาคารก่อนอนุมัติเสมอ
- `auth.js` — ชั้นข้อมูลทั้งหมด เปิดเป็น `window.QQ`
- `i18n.js` — คำแปลไทย/อังกฤษ + ฟังก์ชัน `t()` และ `money()`
- `ui.js` — ตัวช่วยหน้าเว็บ: โหลดรูปสินค้าตอนเลื่อนถึง + ปิดกล่องลอยด้วย Esc/กดพื้นที่มืด
  + ปุ่มคัดลอก (`.copy`) ทุกปุ่มในเว็บ (มีทางสำรองให้เบราว์เซอร์ที่ใช้ clipboard ไม่ได้)
  + ตามโหมดมืดของเครื่องเมื่อผู้ใช้สลับระหว่างเปิดหน้าค้างไว้
- `shop-config.js` — บัญชีรับเงินทุกช่องทาง + URL ของ Worker + ช่องทางติดต่อแอดมิน (`SHOP.contact`)
  + หมวดหมู่สินค้าหน้าร้าน (`CATEGORIES`)
  + **เงื่อนไขเคลม (`SHOP.policy.claimMinutes`)** — แก้ตัวเลขที่นี่ที่เดียว
  หน้าต่างสั่งซื้อ ประวัติการซื้อ และ terms.html เปลี่ยนตามทุกจุดทั้งสองภาษา
- `contact.js` — ปุ่ม "ติดต่อแอดมิน" ลอยมุมจอ + กล่องช่องทางติดต่อ (โหลดหน้าไหน หน้านั้นมีปุ่มเลย)
- `privacy.html` + `terms.html` — นโยบายความเป็นส่วนตัว / เงื่อนไขการให้บริการ+คืนสินค้า
  (สองภาษาในไฟล์เดียว สลับด้วย `data-lang-block` · ตัวเลขเงื่อนไขมาจาก `SHOP.policy`)
- `firestore.rules` — กฎความปลอดภัย (deploy ด้วย `firebase deploy --only firestore:rules`)
- `firestore.indexes.json` — index ที่ประวัติของลูกค้าต้องใช้ (uid + createdAt)
- `og-image.jpg` — รูปที่ขึ้นตอนแชร์ลิงก์ (1200x630) · `favicon.svg` + `favicon-32/96/180.png` — ไอคอนเว็บ
- `robots.txt` + `sitemap.xml` — บอกตัวค้นหาว่าเก็บหน้าไหนได้บ้าง
- `tools/make-share-images.mjs` — สร้างรูปแชร์/ไอคอนใหม่จาก hero-banner.jpg + cat.png
  (`cd tools && npm i --no-save jimp && node make-share-images.mjs`)
- `tests/` — ชุดทดสอบทุกหน้า รันด้วย `cd tests && npm test` (ไม่แตะฐานข้อมูลจริง)

## โครงสร้างข้อมูล (Firestore)
- `users/{uid}` — email, name, phone, provider, **role**, **credit**
- `products/{id}` — name, price, stock, active, **digital**, hasImage, **category**, askUid, askLogin
  (category = รหัสหมวดจาก `CATEGORIES` ใน shop-config.js — ค่าว่าง = ยังไม่ระบุหมวด
   สินค้าเก่าที่ไม่มีฟิลด์นี้ต้องยังโผล่ในแท็บ "ทั้งหมด" เสมอ)
- `productImages/{id}` — image (data URL) ของสินค้ารหัสนั้น **แยกออกมาเพื่อไม่ให้หน้าร้านหนัก**
- `products/{id}/stockItems/{itemId}` — login, password, note, status (available/sold)
- `orders/{id}` — uid, items[], total, **status**, **paid**, **kind**, claimTimerStartedAt
  - **สถานะของระบบใหม่ 4 แบบ:**
    `pending` รอดำเนินการ → `processing` กำลังดำเนินการ → `completed` สำเร็จ · หรือ `cancelled` ยกเลิก+คืนเครดิตแล้ว
  - **สถานะของระบบเก่าที่ยังค้างอยู่:** `approved` / `rejected` — ต้องรองรับต่อไปเสมอ
    (ที่ไหนนับ "ขายสำเร็จ" ต้องนับ `completed` + `approved` · นับ "ไม่ได้ขาย" ต้องนับ `cancelled` + `rejected`)
  - `paid: true` = **เครดิตถูกหักไปแล้วตอนลูกค้ากดสั่ง** (ออเดอร์ระบบใหม่) · ไม่มีฟิลด์นี้ = ออเดอร์เก่า
  - `kind` = `digital` (ไอดีเกม จบในตัว) / `topup` (เติมเกม ต้องรอแอดมิน)
  - `claimTimerStartedAt` = เวลาเริ่มนับเวลาเคลม (ไอดีเกม = ตอนสั่ง · เติมเกม = ตอนแอดมินกด "ทำเสร็จแล้ว")
  - `hiddenAt` = **ซ่อนจากหน้าประวัติของลูกค้า** (null/ไม่มีฟิลด์ = แสดงตามปกติ)
    ไม่ใช่การลบ — ยอดขาย ประวัติหลังบ้าน และ adminLogs ไม่เปลี่ยนเลย
  - `infoEdits[]` = ประวัติที่ลูกค้าแก้ไอดีเกมเอง (เวลา + ค่าเดิม · รหัสผ่านเก็บเป็น `***` ไม่เก็บของจริง)
  (items[] ของเติมเกมมี gameUid / gameLogin / gamePassword ที่ลูกค้ากรอกตอนสั่ง
   ระบบลบ gameLogin/gamePassword ทิ้งอัตโนมัติตอนแอดมินกด "ทำเสร็จแล้ว" → customerInfoClearedAt
   ปุ่มลบด้วยมือยังมีอยู่สำหรับออเดอร์เก่า)
- `topups/{id}` — uid, amount, method, hasSlip, angpaoLink, status, **hiddenAt**
  (สถานะ `processing` = บอทกำลังกดรับซองอยู่ ยังไม่จบ)
- `topupSlips/{topupId}` — slip (data URL) **แยกออกมาเพื่อไม่ให้ตารางหลังบ้านหนัก**
- `settings/shop` — salesResetAt (จุดเริ่มนับยอดขายใหม่)
- `ratelimits/{uid}` — ตัวนับกันยิงบอทรัว
- `adminLogs/{id}` — บันทึกทุกการกระทำของแอดมิน (ปรับเครดิต/อนุมัติ/ตั้งสิทธิ์)
  เขียนได้จากเซิร์ฟเวอร์เท่านั้น อ่านได้เฉพาะแอดมิน แก้/ลบไม่ได้เลย

> เอกสารรุ่นเก่าที่ยังฝัง `image` / `slip` ไว้ในตัวเอง ต้องรองรับต่อไปเสมอ

## กฎเหล็ก — อย่าแก้โดยไม่เข้าใจเหตุผล
1. **ห้ามให้เบราว์เซอร์แตะ `orders` เองเลย** — กฎเป็น `allow create, update, delete: if false`
   ทุกอย่างต้องผ่าน Worker: สร้างออเดอร์ `/order` · ลูกค้าแก้ไอดีเกมของตัวเอง `/order/edit-info`
   *เหตุผล: เคยให้เบราว์เซอร์ส่งยอดเงินมา ลูกค้าแก้ราคาของ 700 บาทเป็น 1 บาทได้*
   *และที่ไม่เปิดกฎให้ลูกค้าแก้ items เอง เพราะ items เป็นอาร์เรย์ — กฎ Firestore ตรวจทีละสมาชิกไม่ได้
   เปิดให้เขียน items เมื่อไหร่ ลูกค้าก็แก้ qty/price ที่อยู่ในอาร์เรย์เดียวกันได้ด้วย = ปั๊มของฟรี*
2. **ห้ามให้เบราว์เซอร์เขียน `credit` หรือ `role` ของใครทั้งสิ้น — รวมถึงแอดมินเอง**
   กฎ users อนุญาตให้แก้ได้แค่ `name` กับ `phone` เท่านั้น
   เครดิตกับสิทธิ์เปลี่ยนได้ทางเดียวคือผ่าน Worker `/admin/*` (service account ไม่อยู่ใต้กฎ)
   ซึ่งตรวจสิทธิ์เองอีกชั้นและบันทึกลง `adminLogs` ทุกครั้ง
   *เหตุผล: บัญชีแอดมินหลุดมือ = เติมเครดิตให้ตัวเองไม่จำกัด ปิดทางนี้ทิ้งทั้งหมด
   งานแอดมินที่ต้องผ่านเซิร์ฟเวอร์: เริ่มดำเนินการ/ทำเสร็จแล้ว/ยกเลิก+คืนเครดิต ·
   อนุมัติ/ไม่อนุมัติออเดอร์เก่า · อนุมัติ/ไม่อนุมัติเติมเงิน ·
   ปรับเครดิต · ลบข้อมูลลูกค้าในออเดอร์ · ซ่อน/เลิกซ่อนรายการ · ตั้ง/ถอดสิทธิ์แอดมิน*
3. **ค่าที่ลูกค้าเขียนได้ ต้องกรองก่อนใส่ใน `src`/`href` เสมอ** (`safeImg` / `safeLink`)
   *เหตุผล: เคยโดนยัดสคริปต์ผ่านช่องสลิปเข้าไปรันในเบราว์เซอร์แอดมิน*
4. **ทุก query ที่ลูกค้าเรียก ต้องมี `where("uid","==",...)` ตั้งแต่ต้น**
   ดึงทั้งคอลเลกชันแล้วค่อยกรองใน JS จะโดนกฎปฏิเสธทั้งคำสั่ง
5. **ห้ามใส่ orderBy บนฟิลด์ที่บางเอกสารไม่มี** — Firestore จะตัดเอกสารนั้นทิ้งเงียบๆ
6. **รหัสผ่านในคลังสินค้า ลูกค้าอ่านไม่ได้เลย** — เห็นได้เฉพาะสำเนาที่คัดลอกเข้า
   ออเดอร์ของตัวเองตอนแอดมินอนุมัติ
7. **สิทธิ์แอดมิน = custom claim + เอกสาร ต้องครบทั้งคู่** (ไม่มีรายชื่ออีเมลในโค้ดแล้ว)
   - ชั้นที่ 1: claim `admin: true` ในบัญชี Firebase Auth — ตั้งได้จากเซิร์ฟเวอร์เท่านั้น ปลอมไม่ได้
   - ชั้นที่ 2: `users/{uid}.role == 'admin'` — เบราว์เซอร์เขียนไม่ได้เช่นกัน
   ตั้ง/ถอดผ่านหลังบ้าน (เรียก `/admin/role`) · ตั้งคนแรก/กู้คืนด้วย `/admin/bootstrap` + รหัสลับ
   *เหตุผลที่ต้องสองชั้น: โทเคนใบเก่าที่ออกก่อนถอนสิทธิ์ยังมี claim ค้างได้ถึง 1 ชั่วโมง
   พอลด role ในเอกสาร กฎก็ปฏิเสธทันทีโดยไม่ต้องรอโทเคนหมดอายุ*
8. **วันที่ต้องใช้เวลาท้องถิ่น ไม่ใช่ UTC** ในการนับยอดรายวัน (เคยทำให้ออเดอร์ตอนเช้าหายไป)
9. **ห้ามเก็บรูป base64 ไว้ในเอกสารที่ต้องดึงมาทั้งลิสต์** (สินค้า/เติมเงิน)
   ให้แยกไปที่ `productImages` / `topupSlips` แล้วโหลดเฉพาะใบที่ต้องใช้
   *เหตุผล: เคยทำให้หน้าแอดมินต้องโหลด 57 MB และหน้าร้าน 8.6 MB ก่อนแสดงอะไรเลย*
10. **ทุกปุ่มที่ "ตัดสิน" รายการ (อนุมัติ/ไม่อนุมัติ) ต้องเช็คสถานะใน transaction**
   *เหตุผล: กด "ไม่อนุมัติ" ทับออเดอร์ที่อนุมัติไปแล้ว = เครดิตหายโดยไม่มีการคืน*
11. **เงินต้องปัดเป็นทศนิยม 2 ตำแหน่งทุกครั้งที่บวกลบ** (`money2`)
12. **รายการเติมเงินที่ยอดเป็น 0 ห้ามกดอนุมัติผ่าน** ต้องบังคับให้แอดมินใส่ยอดก่อน
13. **เพิ่มคอลัมน์ในตารางหลังบ้าน ต้องใส่ `data-label="ชื่อคอลัมน์"` ที่ `<td>` ด้วย**
   บนมือถือตารางถูกแปลงเป็นการ์ด แล้วเอา data-label มาโชว์เป็นชื่อฟิลด์ ถ้าลืมใส่จะเห็นแต่ค่าลอยๆ
14. **เพิ่มหน้า .html ใหม่ ต้องใส่ชุด SEO ให้ครบ** — title/description/canonical/og: ของหน้านั้นเอง
   หน้าที่ต้องล็อกอินก่อนใช้ ใส่ `<meta name="robots" content="noindex, nofollow">` ด้วย
   หน้าสาธารณะเพิ่มใน `sitemap.xml` (อัปเดต `lastmod` ด้วย) แล้วเพิ่มชื่อไฟล์ใน `PAGES` ของ `tests/t21-seo.mjs`
   *เหตุผล: ลืมแล้วแชร์ลิงก์ในเฟซบุ๊กจะขึ้นเป็นกล่องเปล่า และหน้าส่วนตัวหลุดขึ้น Google*
   **และต้องก๊อปสคริปต์โหมดมืดใน `<head>` ไปด้วย** (ก้อนที่มี `prefers-color-scheme`)
   วางไว้ **ก่อน** `<link rel="stylesheet" href="style.css">` เสมอ
   *เหตุผล: ถ้าไม่มี หน้านั้นจะไม่ตามโหมดมืดของเครื่อง · ถ้าวางหลังไฟล์สไตล์ จะเห็นพื้นขาวแวบก่อน
   (เทส `tests/t29-dark-mode.mjs` ตรวจให้ทุกหน้า)*
15. **จอ 320px ต้องไม่เลื่อนซ้ายขวาได้** — กริดที่ใช้ `minmax(320px,...)` ต้องมีกฎมือถือทับให้เหลือคอลัมน์เดียว
   และกล่องที่ใส่ข้อความไทยยาวๆ ต้องมี `min-width:0` ไม่งั้นถูกดันกว้างจนล้นจอ
16. **ออเดอร์มี 2 ระบบอยู่ด้วยกัน ห้ามสลับปุ่มกันเด็ดขาด**
   - `paid !== true` (ออเดอร์เก่า) — **ยังไม่ได้หักเครดิต** ใช้ปุ่ม "อนุมัติ / ไม่อนุมัติ" (`/admin/order/approve|reject`)
   - `paid === true` (ออเดอร์ใหม่) — **หักเครดิตไปแล้วตอนลูกค้ากดสั่ง** ใช้ปุ่ม
     "เริ่มดำเนินการ / ทำเสร็จแล้ว / ยกเลิก+คืนเครดิต" (`/admin/order/start|complete|cancel`)
   *เหตุผล: เอาปุ่ม "อนุมัติ" ไปกดกับออเดอร์ใหม่ = หักเครดิตลูกค้าซ้ำสองรอบ
   เซิร์ฟเวอร์กันไว้อีกชั้นด้วยรหัส `NEW_FLOW_ORDER` / `OLD_ORDER` แต่หน้าเว็บต้องไม่วาดปุ่มผิดชุดตั้งแต่แรก*
17. **กดข้ามขั้นไม่ได้** — `pending → processing → completed` ต้องไล่ทีละขั้น
   *เหตุผล: ลูกค้าแก้ไอดีเกมได้เฉพาะตอน pending ถ้าลัดไป completed เลย ลูกค้าจะยังแก้ได้อยู่
   ตอนที่แอดมินเติมเข้าไอดีเดิมไปแล้ว แล้วมาเถียงกันทีหลังว่าเติมผิดไอดี*

18. **"ซ่อนจากลูกค้า" คือการซ่อนที่หน้าจอ ไม่ใช่การปิดสิทธิ์อ่าน**
   `hiddenAt` ทำให้ `purchases.js` / `wallet.js` ข้ามรายการนั้นไป แต่กฎ Firestore
   ยังให้เจ้าของอ่านเอกสารของตัวเองได้อยู่
   *เหตุผลที่ปิดในกฎไม่ได้: ถ้าเขียนกฎว่า "อ่านได้เมื่อ hiddenAt เป็น null" คำสั่ง list
   ของหน้าประวัติต้องมี `where("hiddenAt","==",null)` ตาม — แล้วเอกสารเก่าทุกใบที่ไม่มีฟิลด์นี้
   จะหายไปจากประวัติลูกค้าทั้งหมด (กฎเหล็กข้อ 5 เรื่องเดียวกัน)*
   ใช้กับ "รายการที่รกหน้าจอ / ลูกค้าขอให้เอาออก" ไม่ใช่ "ความลับที่ห้ามลูกค้าเห็นเด็ดขาด"

## การทดสอบ (สำคัญ)
- **ต้องทดสอบด้วยบัญชีสมาชิกธรรมดา ไม่ใช่บัญชีแอดมิน** — แอดมินอ่านได้ทุกอย่าง
  บั๊กเรื่องสิทธิ์จะถูกซ่อน เคยหลุดขึ้น production มาแล้ว
- `cd tests && npm test` ก่อนเสมอ — จำลองกฎความปลอดภัยครบทั้งไฟล์ ทั้งบทบาทสมาชิกและแอดมิน
  **ถ้าแก้ `firestore.rules` ต้องแก้ `tests/fake/store.mjs` ให้ตรงกันด้วย**
- ทดสอบยิงจริงกับ Firestore (rules simulator ไม่จับเคส list query)
- `tests/t24-admin-security.mjs` = ชุดเจาะช่องโหว่เครดิต/สิทธิ์ ทุกข้อคือสิ่งที่ "ต้องทำไม่ได้"
  เพิ่มความสามารถให้แอดมินเมื่อไหร่ ต้องเพิ่มข้อทดสอบว่าคนอื่นทำไม่ได้ด้วยเสมอ
- **ฐานข้อมูลนี้มีลูกค้าใช้จริง — ลบข้อมูลทดสอบทุกครั้งหลังเสร็จ**
  บัญชีจริงที่ต้องเก็บไว้: `908wayu@gmail.com`, `daas09164@gmail.com`

## Deploy
- หน้าเว็บ: `git push` แล้ว GitHub Pages อัปเดตเองใน ~1 นาที
- กฎความปลอดภัย: `firebase deploy --only firestore:rules --project qqshop-ecc92`
- Worker: `cd worker && npx wrangler deploy`
- ค่าลับของ Worker (ตั้งครั้งเดียว): `SA_KEY` · `RECEIVE_PHONE` · `ADMIN_BOOTSTRAP`
  ตั้งด้วย `npx wrangler secret put <ชื่อ>`
- service account `angpao-bot@qqshop-ecc92.iam.gserviceaccount.com` ต้องมี 2 สิทธิ์:
  **Cloud Datastore User** (ฐานข้อมูล) + **Firebase Authentication Admin** (ตั้ง custom claim)
  เช็คได้ด้วย `/admin/diagnose` (ต้องใส่รหัส ADMIN_BOOTSTRAP)
- ทุกครั้งที่ deploy เสร็จ แจ้งลิงก์เว็บให้ผู้ใช้เสมอ

## กฎอื่นๆ
- ก่อนลบไฟล์หรือเขียนทับไฟล์ที่มีเนื้อหาสำคัญอยู่แล้ว ต้องถามก่อนเสมอ
- ถ้ามีทางเลือกทางเทคนิคที่กระทบการใช้งานจริง ให้ถามสั้นๆ ครั้งเดียวแล้วทำต่อ ไม่ถามซ้ำเรื่องเดิม
- งานที่ยังไม่ได้ทำ + วิธีขายไอดีเกม อยู่ใน `TODO.md`
